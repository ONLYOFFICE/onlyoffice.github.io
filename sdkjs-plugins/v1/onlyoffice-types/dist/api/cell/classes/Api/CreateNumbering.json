{
  "description": "Creates a bullet for a paragraph with the numbering character or symbol specified with the numType parameter.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/spreadsheet-api/Api/Methods/CreateNumbering/",
  "examples": [
    "// How do I automatically number each paragraph in a list inside a shape in a spreadsheet?\n\n// Turn separate paragraphs into an ordered list by attaching numbered labels to each one in a spreadsheet.\n\nlet worksheet = Api.GetActiveSheet();\nlet fill = Api.CreateSolidFill(Api.RGB(255, 111, 61));\nlet stroke = Api.CreateStroke(0, Api.CreateNoFill());\nlet shape = worksheet.AddShape(\"flowChartOnlineStorage\", 120 * 36000, 35 * 36000, fill, stroke, 0, 2 * 36000, 0, 3 * 36000);\nlet docContent = shape.GetContent();\nlet paragraph = docContent.GetElement(0);\nlet bullet = Api.CreateNumbering(\"ArabicParenR\", 1);\nparagraph.SetBullet(bullet);\nparagraph.AddText(\" This is an example of the numbered paragraph.\");\nparagraph = Api.CreateParagraph();\nparagraph.SetBullet(bullet);\nparagraph.AddText(\" This is an example of the numbered paragraph.\");\ndocContent.Push(paragraph);"
  ],
  "params": [
    {
      "description": "The numbering type the paragraphs will be numbered with.",
      "name": "numType",
      "type": "BulletType"
    },
    {
      "description": "The number the first numbered paragraph will start with.",
      "name": "startAt",
      "type": "number"
    }
  ],
  "returns": {
    "type": "ApiBullet"
  },
  "signature": "CreateNumbering(numType: BulletType, startAt: number): ApiBullet"
}
