{
  "description": "Converts points to EMUs (English Metric Units).",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/spreadsheet-api/Api/Methods/PointsToEmus/",
  "examples": [
    "// How do I express a point value as English metric units in a spreadsheet?\n\n// Get the EMU equivalent of a given number of points in a spreadsheet.\n\nconst points = 1000;\nconst emus = Api.PointsToEmus(points);\nconst text = points + ' points are equal to ' + emus + ' english metric units.';\n\nlet worksheet = Api.GetActiveSheet();\nworksheet.GetRange('B2').SetValue(text);"
  ],
  "params": [
    {
      "description": "The number of points to convert to EMUs.",
      "name": "pt",
      "type": "number"
    }
  ],
  "returns": {
    "type": "number"
  },
  "signature": "PointsToEmus(pt: number): number"
}
