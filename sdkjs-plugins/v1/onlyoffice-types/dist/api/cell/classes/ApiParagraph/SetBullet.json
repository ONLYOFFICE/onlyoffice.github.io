{
  "description": "Sets the bullet or numbering to the current paragraph.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/spreadsheet-api/ApiParaPr/Methods/SetBullet/",
  "examples": [
    "// How do I prepend a dash or symbol to lines of text in a paragraph in a spreadsheet?\n\n// Mark each paragraph entry with a custom bullet character to create a visual list in a spreadsheet.\n\nlet worksheet = Api.GetActiveSheet();\nlet fill = Api.CreateSolidFill(Api.RGB(255, 111, 61));\nlet stroke = Api.CreateStroke(0, Api.CreateNoFill());\nlet shape = worksheet.AddShape(\"flowChartOnlineStorage\", 120 * 36000, 35 * 36000, fill, stroke, 0, 2 * 36000, 0, 3 * 36000);\nlet content = shape.GetContent();\nlet paragraph = content.GetElement(0);\nlet paraPr = paragraph.GetParaPr();\nlet bullet = Api.CreateBullet(\"-\");\nparaPr.SetBullet(bullet);\nparagraph.AddText(\" This is an example of the bulleted paragraph.\");"
  ],
  "params": [
    {
      "description": "The bullet object created with the {@link Api#CreateBullet} or {@link Api#CreateNumbering} method.",
      "name": "oBullet",
      "type": "ApiBullet"
    }
  ],
  "returns": {
    "type": "void"
  },
  "signature": "SetBullet(oBullet: ApiBullet): void"
}
