{
  "description": "Sets all 4 font slots with the specified font family.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/spreadsheet-api/ApiParagraph/Methods/SetFontFamily/",
  "examples": [
    "// How do I choose a specific font for an entire paragraph in a spreadsheet?\n\n// Give a paragraph a consistent look by assigning it a named font family in a spreadsheet.\n\nconst worksheet = Api.GetActiveSheet();\nconst fill = Api.CreateSolidFill(Api.RGB(100, 150, 150));\nconst stroke = Api.CreateStroke(0, Api.CreateNoFill());\nconst shape = worksheet.AddShape(\n\t'roundRect',\n\tApi.MillimetersToEmus(70), Api.MillimetersToEmus(30),\n\tfill, stroke,\n\t0, 0, 2, 0\n);\n\nconst content = shape.GetContent();\nconst paragraph = content.GetElement(0);\nparagraph.AddText('This text uses the Georgia font family.');\nparagraph.SetFontFamily('Georgia');\n\nconst paragraph2 = Api.CreateParagraph();\nparagraph2.AddText('This text uses the default font family.');\ncontent.Push(paragraph2);"
  ],
  "params": [
    {
      "description": "The font family or families used for the current paragraph.",
      "name": "sFontFamily",
      "type": "string"
    }
  ],
  "returns": {
    "description": "this",
    "type": "ApiParagraph"
  },
  "signature": "SetFontFamily(sFontFamily: string): ApiParagraph"
}
