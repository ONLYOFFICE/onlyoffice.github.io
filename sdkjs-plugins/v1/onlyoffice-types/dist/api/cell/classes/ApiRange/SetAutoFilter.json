{
  "description": "Adds an AutoFilter to the current range.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/spreadsheet-api/ApiRange/Methods/SetAutoFilter/",
  "examples": [
    "// How do I let users filter rows by value within a range in a spreadsheet?\n\n// Narrow visible rows down to only those matching chosen values in a spreadsheet.\n\nlet worksheet = Api.GetActiveSheet();\nworksheet.GetRange(\"A1\").SetValue(\"header\");\nworksheet.GetRange(\"A2\").SetValue(\"value2\");\nworksheet.GetRange(\"A3\").SetValue(\"value3\");\nworksheet.GetRange(\"A4\").SetValue(\"value4\");\nworksheet.GetRange(\"A5\").SetValue(\"value5\");\nlet range = worksheet.GetRange(\"A1:A5\");\nrange.SetAutoFilter(1, [\"value2\",\"value3\"], \"xlFilterValues\");"
  ],
  "params": [
    {
      "description": "The integer offset of the field on which you want to base the filter (from the left of the list; the leftmost field is field one). If {null} provided, clears the AutoFilter for the range.",
      "name": "Field",
      "optional": true,
      "type": "number | null"
    },
    {
      "description": "The criteria (a string; for example, \"101\"). Use \"=\" to find blank fields, \"<>\" to find non-blank fields, and \"><\" to select (No Data) fields in data types.\rIf this argument is omitted, the criteria is All. If Operator is xlTop10Items, Criteria1 specifies the number of items (for example, \"10\").",
      "name": "Criteria1",
      "optional": true,
      "type": "string | string[] | ApiColor | XlDynamicFilterCriteria"
    },
    {
      "description": "An XlAutoFilterOperator constant specifying the type of filter.",
      "name": "Operator",
      "optional": true,
      "type": "XlAutoFilterOperator"
    },
    {
      "description": "The second criteria (a string). Used with Criteria1 and Operator to construct compound criteria.",
      "name": "Criteria2",
      "optional": true,
      "type": "string"
    },
    {
      "description": "True to display the AutoFilter drop-down arrow for the filtered field. False to hide the AutoFilter drop-down arrow for the filtered field. True by default.",
      "name": "VisibleDropDown",
      "optional": true,
      "type": "boolean"
    }
  ],
  "returns": {
    "type": "void"
  },
  "signature": "SetAutoFilter(Field?: number | null, Criteria1?: string | string[] | ApiColor | XlDynamicFilterCriteria, Operator?: XlAutoFilterOperator, Criteria2?: string, VisibleDropDown?: boolean): void",
  "since": "8.3.0"
}
