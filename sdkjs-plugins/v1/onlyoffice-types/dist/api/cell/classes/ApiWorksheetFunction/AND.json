{
  "description": "Checks whether all conditions in a test are **true**.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/spreadsheet-api/ApiWorksheetFunction/Methods/AND/",
  "examples": [
    "// Use the AND function to verify multiple criteria are met in a spreadsheet.\n\n// Validate that every condition passes together to get a true/false result in a spreadsheet.\n\nconst worksheet = Api.GetActiveSheet();\n\nlet func = Api.WorksheetFunction;\nlet ans = func.AND(12 < 100, 34 < 100, 50 < 100); //AND logical function\n\nworksheet.GetRange(\"C1\").SetValue(ans);"
  ],
  "params": [
    {
      "description": "A condition to check.",
      "name": "args",
      "type": "number | string | ApiRange | boolean | ApiName"
    }
  ],
  "returns": {
    "type": "boolean"
  },
  "signature": "AND(args: number | string | ApiRange | boolean | ApiName): boolean"
}
