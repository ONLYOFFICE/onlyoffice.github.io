{
  "description": "Returns a bitwise \"OR\" of two numbers.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/spreadsheet-api/ApiWorksheetFunction/Methods/BITOR/",
  "examples": [
    "// Which bits are set in either number in a spreadsheet?\n\n// Merge binary values to include all active bits in a spreadsheet.\n\nlet worksheet = Api.GetActiveSheet();\nlet func = Api.WorksheetFunction;\nworksheet.GetRange(\"A1\").SetValue(func.BITOR(23, 10));"
  ],
  "params": [
    {
      "description": "The first decimal representation of the binary number to evaluate.",
      "name": "arg1",
      "type": "ApiRange | ApiName | number"
    },
    {
      "description": "The second decimal representation of the binary number to evaluate.",
      "name": "arg2",
      "type": "ApiRange | ApiName | number"
    }
  ],
  "returns": {
    "type": "number"
  },
  "signature": "BITOR(arg1: ApiRange | ApiName | number, arg2: ApiRange | ApiName | number): number"
}
