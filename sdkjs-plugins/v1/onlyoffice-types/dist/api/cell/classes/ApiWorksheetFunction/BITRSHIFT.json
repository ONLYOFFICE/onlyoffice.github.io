{
  "description": "Returns a number shifted right by the specified number of bits.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/spreadsheet-api/ApiWorksheetFunction/Methods/BITRSHIFT/",
  "examples": [
    "// What happens when you move the binary representation rightward in a spreadsheet?\n\n// Divide a value by moving its bits toward lower positions in a spreadsheet.\n\nlet worksheet = Api.GetActiveSheet();\nlet func = Api.WorksheetFunction;\nworksheet.GetRange(\"A1\").SetValue(func.BITRSHIFT(13, 2));"
  ],
  "params": [
    {
      "description": "The decimal representation of the binary number to evaluate.",
      "name": "arg1",
      "type": "ApiRange | ApiName | number"
    },
    {
      "description": "The number of bits by which the number will be shifted right.",
      "name": "arg2",
      "type": "ApiRange | ApiName | number"
    }
  ],
  "returns": {
    "type": "number"
  },
  "signature": "BITRSHIFT(arg1: ApiRange | ApiName | number, arg2: ApiRange | ApiName | number): number"
}
