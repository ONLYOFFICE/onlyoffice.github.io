{
  "description": "Returns a bitwise \"XOR\" (Exclusive Or) of two numbers.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/spreadsheet-api/ApiWorksheetFunction/Methods/BITXOR/",
  "examples": [
    "// Which bits are active in one number but not the other in a spreadsheet?\n\n// Highlight the unique bits when comparing two values in a spreadsheet.\n\nlet worksheet = Api.GetActiveSheet();\nlet func = Api.WorksheetFunction;\nworksheet.GetRange(\"A1\").SetValue(func.BITXOR(5, 3));"
  ],
  "params": [
    {
      "description": "The first decimal representation of the binary number to evaluate.",
      "name": "arg1",
      "type": "ApiRange | ApiName | number"
    },
    {
      "description": "The second decimal representation of the binary number to evaluate.",
      "name": "arg2",
      "type": "ApiRange | ApiName | number"
    }
  ],
  "returns": {
    "type": "number"
  },
  "signature": "BITXOR(arg1: ApiRange | ApiName | number, arg2: ApiRange | ApiName | number): number"
}
