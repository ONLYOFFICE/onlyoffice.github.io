{
  "description": "Returns a number that is rounded up to the nearest integer or to the nearest multiple of significance. The number is always rounded up regardless of its sing.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/spreadsheet-api/ApiWorksheetFunction/Methods/CEILING_PRECISE/",
  "examples": [
    "// What is the precise ceiling for positive and negative values in a spreadsheet?\n\n// Always round toward positive infinity by a specified interval in a spreadsheet.\n\nlet worksheet = Api.GetActiveSheet();\nlet func = Api.WorksheetFunction;\nworksheet.GetRange(\"A1\").SetValue(func.CEILING_PRECISE(-6.7, 2));"
  ],
  "params": [
    {
      "description": "The value to round up.",
      "name": "arg1",
      "type": "ApiRange | ApiName | number"
    },
    {
      "description": "The multiple of significance to round up to. If it is omitted, the default value of 1 is used. If it is set to zero, the function returns 0.",
      "name": "arg2",
      "optional": true,
      "type": "ApiRange | ApiName | number"
    }
  ],
  "returns": {
    "type": "number"
  },
  "signature": "CEILING_PRECISE(arg1: ApiRange | ApiName | number, arg2?: ApiRange | ApiName | number): number"
}
