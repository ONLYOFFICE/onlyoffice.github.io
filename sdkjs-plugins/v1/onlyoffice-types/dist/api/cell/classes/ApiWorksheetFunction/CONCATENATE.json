{
  "description": "Combines multiple text strings into one text string.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/spreadsheet-api/ApiWorksheetFunction/Methods/CONCATENATE/",
  "examples": [
    "// What happens when you join different text values together in a spreadsheet?\n\n// Link multiple pieces of text to form one combined string in a spreadsheet.\n\nlet worksheet = Api.GetActiveSheet();\nlet func = Api.WorksheetFunction;\nworksheet.GetRange(\"A1\").SetValue(func.CONCATENATE(\"John\", \" \", \"Adams\"));"
  ],
  "params": [
    {
      "description": "Up to 255 data values that will be combined.",
      "name": "arg_n",
      "type": "ApiRange | ApiName | string"
    }
  ],
  "returns": {
    "type": "string"
  },
  "signature": "CONCATENATE(arg_n: ApiRange | ApiName | string): string"
}
