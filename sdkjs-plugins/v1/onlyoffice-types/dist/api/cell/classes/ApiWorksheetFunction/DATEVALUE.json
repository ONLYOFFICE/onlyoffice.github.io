{
  "description": "Converts a date in the form of text to a number that represents the date in the date-time code.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/spreadsheet-api/ApiWorksheetFunction/Methods/DATEVALUE/",
  "examples": [
    "// What date value does a text string represent in a spreadsheet?\n\n// Parse text into a usable date format in a spreadsheet.\n\nconst worksheet = Api.GetActiveSheet();\n\nlet func = Api.WorksheetFunction;\nlet ans = func.DATEVALUE(\"2018-3-16\"); \n\nworksheet.GetRange(\"C1\").SetValue(ans);"
  ],
  "params": [
    {
      "description": "The text that represents a date, between 1/1/1900 or 1/1/1904 (depending on the workbook's date system) and 12/31/9999.",
      "name": "arg1",
      "type": "ApiRange | ApiName | string"
    }
  ],
  "returns": {
    "type": "number"
  },
  "signature": "DATEVALUE(arg1: ApiRange | ApiName | string): number"
}
