{
  "description": "Returns the complementary error function integrated between the specified lower limit and infinity.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/spreadsheet-api/ApiWorksheetFunction/Methods/ERFC/",
  "examples": [
    "// How do I compute the complementary error function in a spreadsheet?\n\n// Evaluate the complement of the error function from a value to infinity in a spreadsheet.\n\nlet worksheet = Api.GetActiveSheet();\nlet func = Api.WorksheetFunction;\nworksheet.GetRange(\"A1\").SetValue(func.ERFC(-2));"
  ],
  "params": [
    {
      "description": "The lower bound for integrating the complementary error function.",
      "name": "arg1",
      "type": "ApiRange | ApiName | number"
    }
  ],
  "returns": {
    "type": "number"
  },
  "signature": "ERFC(arg1: ApiRange | ApiName | number): number"
}
