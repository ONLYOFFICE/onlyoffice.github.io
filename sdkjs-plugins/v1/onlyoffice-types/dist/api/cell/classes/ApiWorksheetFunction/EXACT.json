{
  "description": "Checks whether two text strings are exactly the same, and returns **true** or **false**. This function is case-sensitive.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/spreadsheet-api/ApiWorksheetFunction/Methods/EXACT/",
  "examples": [
    "let worksheet = Api.GetActiveSheet();\nlet func = Api.GetWorksheetFunction();\nworksheet.GetRange(\"A1\").SetValue(func.EXACT(\"MyPassword\", \"mypassword\"));"
  ],
  "params": [
    {
      "description": "The first text string.",
      "name": "arg1",
      "type": "ApiRange | ApiName | string"
    },
    {
      "description": "The second text string.",
      "name": "arg2",
      "type": "ApiRange | ApiName | string"
    }
  ],
  "returns": {
    "type": "boolean"
  },
  "signature": "EXACT(arg1: ApiRange | ApiName | string, arg2: ApiRange | ApiName | string): boolean"
}
