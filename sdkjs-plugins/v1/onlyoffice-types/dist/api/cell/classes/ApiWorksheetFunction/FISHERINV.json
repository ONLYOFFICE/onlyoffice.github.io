{
  "description": "Returns the inverse of the Fisher transformation: if y = FISHER(x), then FISHERINV(y) = x.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/spreadsheet-api/ApiWorksheetFunction/Methods/FISHERINV/",
  "examples": [
    "// How do I convert a Fisher transformation back to a correlation in a spreadsheet?\n\n// Extract the original correlation value from its Fisher transformation in a spreadsheet.\n\nlet worksheet = Api.GetActiveSheet();\nlet func = Api.WorksheetFunction;\nlet ans = func.FISHERINV(0.56);\nworksheet.GetRange(\"B2\").SetValue(ans);"
  ],
  "params": [
    {
      "description": "The value to perform the inverse of the transformation.",
      "name": "arg1",
      "type": "ApiRange | ApiName | number"
    }
  ],
  "returns": {
    "type": "number"
  },
  "signature": "FISHERINV(arg1: ApiRange | ApiName | number): number"
}
