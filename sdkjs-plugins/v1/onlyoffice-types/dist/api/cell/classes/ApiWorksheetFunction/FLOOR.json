{
  "description": "Rounds a number down to the nearest multiple of significance.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/spreadsheet-api/ApiWorksheetFunction/Methods/FLOOR/",
  "examples": [
    "// How do I round a number down to a specific increment in a spreadsheet?\n\n// Decrease a number to the nearest multiple of a given significance in a spreadsheet.\n\nlet worksheet = Api.GetActiveSheet();\nlet func = Api.WorksheetFunction;\nworksheet.GetRange(\"A1\").SetValue(func.FLOOR(5.786, 0.7));"
  ],
  "params": [
    {
      "description": "The numeric value to round down.",
      "name": "arg1",
      "type": "ApiRange | ApiName | number"
    },
    {
      "description": "The multiple of significance to round down to. The number to round down and the multiple of significance must have the same sign.",
      "name": "arg2",
      "type": "ApiRange | ApiName | number"
    }
  ],
  "returns": {
    "type": "number"
  },
  "signature": "FLOOR(arg1: ApiRange | ApiName | number, arg2: ApiRange | ApiName | number): number"
}
