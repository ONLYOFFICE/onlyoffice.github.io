{
  "description": "Returns the gamma function value.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/spreadsheet-api/ApiWorksheetFunction/Methods/GAMMA/",
  "examples": [
    "// Calculate the gamma function for the given parameter value.\n\n// Determine gamma function result using GAMMA with numeric input.\n\nlet worksheet = Api.GetActiveSheet();\nlet func = Api.WorksheetFunction;\nlet ans = func.GAMMA(2.7);\nworksheet.GetRange(\"B2\").SetValue(ans);"
  ],
  "params": [
    {
      "description": "The value for which the gamma function will be calculated.",
      "name": "arg1",
      "type": "ApiRange | ApiName | number"
    }
  ],
  "returns": {
    "type": "number"
  },
  "signature": "GAMMA(arg1: ApiRange | ApiName | number): number"
}
