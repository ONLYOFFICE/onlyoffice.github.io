{
  "description": "Tests whether a number is greater than a threshold value. The function returns 1 if the number is greater than or equal to the threshold value and 0 otherwise.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/spreadsheet-api/ApiWorksheetFunction/Methods/GESTEP/",
  "examples": [
    "// How do I test whether a value meets a minimum requirement in a spreadsheet?\n\n// Get a binary result showing if one number exceeds another in a spreadsheet.\n\nlet worksheet = Api.GetActiveSheet();\nlet func = Api.WorksheetFunction;\nworksheet.GetRange(\"A1\").SetValue(func.GESTEP(-2, 2));"
  ],
  "params": [
    {
      "description": "The value to test against step.",
      "name": "arg1",
      "type": "ApiRange | ApiName | number"
    },
    {
      "description": "The threshold value.",
      "name": "arg2",
      "optional": true,
      "type": "ApiRange | ApiName | number"
    }
  ],
  "returns": {
    "type": "number"
  },
  "signature": "GESTEP(arg1: ApiRange | ApiName | number, arg2?: ApiRange | ApiName | number): number"
}
