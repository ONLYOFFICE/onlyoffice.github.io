{
  "description": "Checks whether a value is a logical value (**true** or **false**), and returns **true** or **false**.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/spreadsheet-api/ApiWorksheetFunction/Methods/ISLOGICAL/",
  "examples": [
    "const worksheet = Api.GetActiveSheet();\nworksheet.GetRange(\"B3\").SetValue(\"66\");\n\nlet func = Api.GetWorksheetFunction();\nlet result = func.ISLOGICAL(worksheet.GetRange(\"B3\"));\nworksheet.GetRange(\"C3\").SetValue(result);"
  ],
  "params": [
    {
      "description": "The value to test.\rThe value can be an empty cell, error, logical value, text, number, range, or range name.",
      "name": "arg1",
      "type": "ApiRange | string | number | boolean | ApiName"
    }
  ],
  "returns": {
    "type": "boolean"
  },
  "signature": "ISLOGICAL(arg1: ApiRange | string | number | boolean | ApiName): boolean"
}
