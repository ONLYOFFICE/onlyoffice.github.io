{
  "description": "Checks whether a value is a number, and returns **true** or **false**.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/spreadsheet-api/ApiWorksheetFunction/Methods/ISNUMBER/",
  "examples": [
    "let worksheet = Api.GetActiveSheet();\nlet func = Api.GetWorksheetFunction();\nworksheet.GetRange(\"A1\").SetValue(func.ISNUMBER(\"#N/A\"));\nworksheet.GetRange(\"A2\").SetValue(func.ISNUMBER(255));\nworksheet.GetRange(\"A3\").SetValue(func.ISNUMBER(\"Online Office\"));"
  ],
  "params": [
    {
      "description": "The value to test.\rThe value can be an empty cell, error, logical value, text, number, range, or range name.",
      "name": "arg1",
      "type": "ApiRange | string | number | boolean | ApiName"
    }
  ],
  "returns": {
    "type": "boolean"
  },
  "signature": "ISNUMBER(arg1: ApiRange | string | number | boolean | ApiName): boolean"
}
