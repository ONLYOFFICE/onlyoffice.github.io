{
  "description": "Returns **true** if a number is odd.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/spreadsheet-api/ApiWorksheetFunction/Methods/ISODD/",
  "examples": [
    "// How do I determine whether a number is odd or even in a spreadsheet?\n\n// Use a function to filter odd numbers from a set of values in a spreadsheet.\n\nconst worksheet = Api.GetActiveSheet();\n\nlet func = Api.WorksheetFunction;\nlet result = func.ISODD(\"94\");\nworksheet.GetRange(\"C3\").SetValue(result);"
  ],
  "params": [
    {
      "description": "The value to test.",
      "name": "arg1",
      "type": "ApiRange | ApiName | number"
    }
  ],
  "returns": {
    "type": "boolean"
  },
  "signature": "ISODD(arg1: ApiRange | ApiName | number): boolean"
}
