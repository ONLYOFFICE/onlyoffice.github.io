{
  "description": "Checks whether a value is a reference, and returns **true** or **false**.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/spreadsheet-api/ApiWorksheetFunction/Methods/ISREF/",
  "examples": [
    "// How do I verify if a value points to a cell or range in a spreadsheet?\n\n// Use a function to distinguish between cell references and literal values in a spreadsheet.\n\nlet worksheet = Api.GetActiveSheet();\nlet func = Api.WorksheetFunction;\nworksheet.GetRange(\"A1\").SetValue(func.ISREF(\"A3\"));\nworksheet.GetRange(\"A2\").SetValue(func.ISREF(worksheet.GetRange(\"A3\")));"
  ],
  "params": [
    {
      "description": "The value to test.\rThe value can be an empty cell, error, logical value, text, number, range, or range name.",
      "name": "arg1",
      "type": "ApiRange | string | number | boolean | ApiName"
    }
  ],
  "returns": {
    "type": "boolean"
  },
  "signature": "ISREF(arg1: ApiRange | string | number | boolean | ApiName): boolean"
}
