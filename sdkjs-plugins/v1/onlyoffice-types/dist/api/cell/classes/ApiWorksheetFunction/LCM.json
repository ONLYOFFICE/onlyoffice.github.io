{
  "description": "Returns the least common multiple.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/spreadsheet-api/ApiWorksheetFunction/Methods/LCM/",
  "examples": [
    "// How do I find the smallest number that all values divide evenly into in a spreadsheet?\n\n// Get the least common multiple for use in fraction operations in a spreadsheet.\n\nlet worksheet = Api.GetActiveSheet();\nlet func = Api.WorksheetFunction;\nworksheet.GetRange(\"A1\").SetValue(func.LCM(4, 6, 8, 8, 12, 24, 2, 1));"
  ],
  "params": [
    {
      "description": "Up to 255 numeric values for which the least common multiple will be returned. The first argument is required, subsequent arguments are optional.",
      "name": "args",
      "type": "ApiRange | ApiName | number"
    }
  ],
  "returns": {
    "type": "number"
  },
  "signature": "LCM(args: ApiRange | ApiName | number): number"
}
