{
  "description": "Returns a number rounded to the desired multiple.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/spreadsheet-api/ApiWorksheetFunction/Methods/MROUND/",
  "examples": [
    "// What is a number rounded to a specific multiple in a spreadsheet?\n\n// Adjust a value to the closest increment in a spreadsheet.\n\nlet worksheet = Api.GetActiveSheet();\nlet func = Api.WorksheetFunction;\nworksheet.GetRange(\"A1\").SetValue(func.MROUND(14.35, 0.4));"
  ],
  "params": [
    {
      "description": "The value to round.",
      "name": "arg1",
      "type": "ApiRange | ApiName | number"
    },
    {
      "description": "The multiple to round the number to.",
      "name": "arg2",
      "type": "ApiRange | ApiName | number"
    }
  ],
  "returns": {
    "type": "number"
  },
  "signature": "MROUND(arg1: ApiRange | ApiName | number, arg2: ApiRange | ApiName | number): number"
}
