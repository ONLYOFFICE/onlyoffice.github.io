{
  "description": "Returns the unit matrix for the specified dimension.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/spreadsheet-api/ApiWorksheetFunction/Methods/MUNIT/",
  "examples": [
    "// Create a unit matrix and place it in a cell in a spreadsheet.\n\n// Insert a matrix of ones and zeros into your spreadsheet.\n\nlet worksheet = Api.GetActiveSheet();\nlet func = Api.WorksheetFunction;\nworksheet.GetRange(\"A1\").SetValue(func.MUNIT(3));"
  ],
  "params": [
    {
      "description": "An integer specifying the dimension of the unit matrix to be returned.",
      "name": "arg1",
      "type": "ApiRange | ApiName | number"
    }
  ],
  "returns": {
    "type": "number"
  },
  "signature": "MUNIT(arg1: ApiRange | ApiName | number): number"
}
