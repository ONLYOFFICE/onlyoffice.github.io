{
  "description": "Converts a value to a number, dates to serial numbers, **true** to 1, error to {@link global#ErrorValue ErrorValue}, anything else to 0 (zero).",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/spreadsheet-api/ApiWorksheetFunction/Methods/N/",
  "examples": [
    "// Transform values into numeric form for calculations in a spreadsheet.\n\n// Change boolean values and dates into numbers in a spreadsheet.\n\nlet worksheet = Api.GetActiveSheet();\nlet func = Api.WorksheetFunction;\nworksheet.GetRange(\"A1\").SetValue(func.N(false));"
  ],
  "params": [
    {
      "description": "The value to be converted. The value can be a logical value, text, or number.",
      "name": "arg1",
      "type": "ApiRange | ApiName | number | string | boolean"
    }
  ],
  "returns": {
    "type": "number"
  },
  "signature": "N(arg1: ApiRange | ApiName | number | string | boolean): number"
}
