{
  "description": "Returns the value of the density function for a standard normal distribution.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/spreadsheet-api/ApiWorksheetFunction/Methods/PHI/",
  "examples": [
    "// How do I get the probability density value for a standard normal distribution in a spreadsheet?\n\n// Find how likely a value is within a bell curve distribution in a spreadsheet.\n\nconst worksheet = Api.GetActiveSheet();\n\n//method params\nlet number = 5;\n\nworksheet.GetRange(\"A1\").SetValue(number);\n\nlet func = Api.WorksheetFunction;\nlet ans = func.PHI(number);\n\nworksheet.GetRange(\"C1\").SetValue(ans);"
  ],
  "params": [
    {
      "description": "The number for which the density of the standard normal distribution will be returned.",
      "name": "arg1",
      "type": "ApiRange | ApiName | number"
    }
  ],
  "returns": {
    "type": "number"
  },
  "signature": "PHI(arg1: ApiRange | ApiName | number): number"
}
