{
  "description": "Returns the rank of a number in a list of numbers: its size relative to other values in the list. If more than one value has the same rank, the average rank is returned.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/spreadsheet-api/ApiWorksheetFunction/Methods/RANK_AVG/",
  "examples": [
    "const worksheet = Api.GetActiveSheet();\n\nlet valueArr = [7, 6, 5, 5];\n\n// Place the numbers in cells\nfor (let i = 0; i < valueArr.length; i++) {\n  worksheet.GetRange(\"A\" + (i + 1)).SetValue(valueArr[i]);\n}\n\n//method params\nlet number = worksheet.GetRange(\"A3\");\nlet range = worksheet.GetRange(\"A1:A4\");\nlet order = 0;\n\nlet func = Api.GetWorksheetFunction();\nlet ans = func.RANK_AVG(number, range, order);\n\nworksheet.GetRange(\"C1\").SetValue(ans);"
  ],
  "params": [
    {
      "description": "The number for which the rank will be returned.",
      "name": "arg1",
      "type": "ApiRange | ApiName | number"
    },
    {
      "description": "An array or range of numbers. Nonnumeric values are ignored.",
      "name": "arg2",
      "type": "ApiRange | ApiName | number[]"
    },
    {
      "description": "The numeric value that specifyes how to order the numbers. If it is 0 or omitted, the rank in the list will be sorted in descending order.\rAny other numeric value means that the rank in the list will be sorted in ascending order.",
      "name": "arg3",
      "optional": true,
      "type": "ApiRange | ApiName | boolean"
    }
  ],
  "returns": {
    "type": "number"
  },
  "signature": "RANK_AVG(arg1: ApiRange | ApiName | number, arg2: ApiRange | ApiName | number[], arg3?: ApiRange | ApiName | boolean): number"
}
