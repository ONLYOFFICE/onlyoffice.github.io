{
  "description": "Rounds a number to a specified number of digits.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/spreadsheet-api/ApiWorksheetFunction/Methods/ROUND/",
  "examples": [
    "// How do I round numbers to a certain number of digits in a spreadsheet?\n\n// Adjust a number's precision by rounding it in a spreadsheet.\n\nlet worksheet = Api.GetActiveSheet();\nlet func = Api.WorksheetFunction;\nworksheet.GetRange(\"A1\").SetValue(func.ROUND(3.456, 2));"
  ],
  "params": [
    {
      "description": "The number to round.",
      "name": "arg1",
      "type": "ApiRange | ApiName | number"
    },
    {
      "description": "The number of digits to round to. If this argument is negative, the number will be rounded to the left of the decimal point.\rIf it is equal to zero, the number will be rounded to the nearest integer.",
      "name": "arg2",
      "type": "ApiRange | ApiName | number"
    }
  ],
  "returns": {
    "type": "number"
  },
  "signature": "ROUND(arg1: ApiRange | ApiName | number, arg2: ApiRange | ApiName | number): number"
}
