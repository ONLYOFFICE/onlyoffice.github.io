{
  "description": "Returns the sheet number of the reference sheet.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/spreadsheet-api/ApiWorksheetFunction/Methods/SHEET/",
  "examples": [
    "// Retrieve the sheet number for a given sheet reference using the SHEET function.\n\n// Apply the function to display the sheet index in a cell.\n\nconst worksheet = Api.GetActiveSheet(); \nlet func = Api.WorksheetFunction;\nlet result = func.SHEET(\"Sheet1\");\nworksheet.GetRange(\"C3\").SetValue(result);"
  ],
  "params": [
    {
      "description": "The name of a sheet or a reference for which the sheet number will be returned. If omitted the number of the sheet containing the function is returned.",
      "name": "arg1",
      "optional": true,
      "type": "string | ApiRange | ApiName"
    }
  ],
  "returns": {
    "type": "number"
  },
  "signature": "SHEET(arg1?: string | ApiRange | ApiName): number"
}
