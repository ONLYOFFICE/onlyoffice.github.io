{
  "description": "Checks whether a value is text, and returns the text if it is, or returns double quotes (empty text) if it is not.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/spreadsheet-api/ApiWorksheetFunction/Methods/T/",
  "examples": [
    "// Test if a value is text using the T function and return accordingly.\n\n// Returns the text value if the input is text, otherwise returns empty string.\n\nlet worksheet = Api.GetActiveSheet();\nlet func = Api.WorksheetFunction;\nworksheet.GetRange(\"A1\").SetValue(func.T(\"date and time\"));"
  ],
  "params": [
    {
      "description": "The value to test.",
      "name": "arg1",
      "type": "ApiRange | ApiName | number | string | boolean"
    }
  ],
  "returns": {
    "type": "ApiRange | ApiName | string"
  },
  "signature": "T(arg1: ApiRange | ApiName | number | string | boolean): ApiRange | ApiName | string"
}
