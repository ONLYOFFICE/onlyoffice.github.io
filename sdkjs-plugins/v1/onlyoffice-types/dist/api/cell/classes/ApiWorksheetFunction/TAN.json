{
  "description": "Returns the tangent of an angle.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/spreadsheet-api/ApiWorksheetFunction/Methods/TAN/",
  "examples": [
    "// Calculate the tangent of an angle using the TAN function.\n\n// Returns the tangent value of the specified angle in radians.\n\nlet worksheet = Api.GetActiveSheet();\nlet func = Api.WorksheetFunction;\nworksheet.GetRange(\"A1\").SetValue(func.TAN(0.5));"
  ],
  "params": [
    {
      "description": "The angle in radians for which the tangent will be returned. If the argument is in degrees, multiply it by *PI()/180*.",
      "name": "arg1",
      "type": "ApiRange | ApiName | number"
    }
  ],
  "returns": {
    "type": "number"
  },
  "signature": "TAN(arg1: ApiRange | ApiName | number): number"
}
