{
  "description": "Returns the number (code point) corresponding to the first character of the text.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/spreadsheet-api/ApiWorksheetFunction/Methods/UNICODE/",
  "examples": [
    "// The UNICODE function converts characters to their numeric Unicode values.\n\n// Apply the function to obtain a character's code point from text in a spreadsheet.\n\nlet worksheet = Api.GetActiveSheet();\nlet func = Api.WorksheetFunction;\nworksheet.GetRange(\"A1\").SetValue(func.UNICODE(\"example\"));"
  ],
  "params": [
    {
      "description": "The character for which the Unicode value will be returned.",
      "name": "arg1",
      "type": "ApiRange | ApiName | string"
    }
  ],
  "returns": {
    "type": "number"
  },
  "signature": "UNICODE(arg1: ApiRange | ApiName | string): number"
}
