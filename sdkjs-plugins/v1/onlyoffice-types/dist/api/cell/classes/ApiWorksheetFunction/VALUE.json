{
  "description": "Converts a text string that represents a number to a number.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/spreadsheet-api/ApiWorksheetFunction/Methods/VALUE/",
  "examples": [
    "// The VALUE function parses numeric text and returns actual numeric values.\n\n// Apply the function to convert numeric text strings to numbers in a spreadsheet.\n\nlet worksheet = Api.GetActiveSheet();\nlet func = Api.WorksheetFunction;\nworksheet.GetRange(\"A1\").SetValue(func.VALUE(\"$3.50\"));"
  ],
  "params": [
    {
      "description": "The text enclosed in quotation marks or a reference to a cell containing the text which will be converted to a number.",
      "name": "arg1",
      "type": "ApiRange | ApiName | string"
    }
  ],
  "returns": {
    "type": "number"
  },
  "signature": "VALUE(arg1: ApiRange | ApiName | string): number"
}
