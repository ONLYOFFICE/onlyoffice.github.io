{
  "description": "Creates an image with the parameters specified.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/pdf-api/Api/Methods/CreateImage/",
  "examples": [
    "// Create an image and insert it to the page.\n\n// Create the image in a PDF document.\n\nconst doc = Api.GetDocument();\nconst page = doc.GetPage(0);\nconst shape = Api.CreateImage(\n\t'https://static.onlyoffice.com/assets/docs/samples/img/onlyoffice_logo.png',\n\t60 * 36000, 60 * 36000\n);\npage.AddObject(shape);"
  ],
  "params": [
    {
      "description": "The image source where the image to be inserted should be taken from (currently,\ronly internet URL or Base64 encoded images are supported).",
      "name": "imageSrc",
      "type": "string"
    },
    {
      "description": "The image width in English measure units.",
      "name": "width",
      "type": "EMU"
    },
    {
      "description": "The image height in English measure units.",
      "name": "height",
      "type": "EMU"
    }
  ],
  "returns": {
    "type": "ApiImage"
  },
  "signature": "CreateImage(imageSrc: string, width: EMU, height: EMU): ApiImage"
}
