{
  "description": "Creates a color selecting it from one of the available color presets.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/pdf-api/Api/Methods/CreatePresetColor/",
  "examples": [
    "// How do I select a named color for a gradient in a PDF?\n\n// Create a gradient stop with a built-in preset color in a PDF.\n\nconst doc = Api.GetDocument();\nconst page = doc.GetPage(0);\n\nconst gs1 = Api.CreateGradientStop(Api.CreatePresetColor(\"peachPuff\"), 0);\nconst gs2 = Api.CreateGradientStop(Api.RGB(255, 111, 61), 100000);\nconst fill = Api.CreateRadialGradientFill([gs1, gs2]);\nconst stroke = Api.CreateStroke(0, Api.CreateNoFill());\nconst drawing = Api.CreateShape(\"flowChartMagneticTape\", 150 * 36000, 65 * 36000, fill, stroke);\ndrawing.SetPosition(608400, 1267200);\npage.AddObject(drawing);"
  ],
  "params": [
    {
      "description": "A preset selected from the list of the available color preset names.",
      "name": "presetColor",
      "type": "PresetColor"
    }
  ],
  "returns": {
    "description": ";",
    "type": "ApiPresetColor"
  },
  "signature": "CreatePresetColor(presetColor: PresetColor): ApiPresetColor"
}
