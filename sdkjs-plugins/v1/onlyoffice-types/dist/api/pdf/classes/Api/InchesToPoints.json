{
  "description": "Converts inches to points.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/pdf-api/Api/Methods/InchesToPoints/",
  "examples": [
    "// How do I convert inches to points for sizing in a PDF?\n\n// Translate inch values to point measurements and show the conversion in a PDF.\n\nconst inches = 100;\nconst points = Api.InchesToPoints(inches);\n\nconst rgb = Api.RGB(50, 150, 250);\nconst fill = Api.CreateSolidFill(rgb);\nconst stroke = Api.CreateStroke(0, Api.CreateNoFill());\nconst shape = Api.CreateShape('rect', 150 * 36000, 150 * 36000, fill, stroke);\nconst paragraph = shape.GetContent().GetElement(0);\nparagraph.AddText(inches + ' inches are equal to ' + points + ' points.');\n\nconst doc = Api.GetDocument();\nconst page = doc.GetPage(0);\npage.AddObject(shape);"
  ],
  "params": [
    {
      "description": "The number of inches to convert to points.",
      "name": "inches",
      "type": "number"
    }
  ],
  "returns": {
    "type": "number"
  },
  "signature": "InchesToPoints(inches: number): number"
}
