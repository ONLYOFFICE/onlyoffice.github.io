{
  "description": "Sets all 4 font slots with the specified font family.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/pdf-api/ApiParagraph/Methods/SetFontFamily/",
  "examples": [
    "// How do I change the font family in a paragraph in a PDF?\n\n// Apply a specific typeface to a paragraph in a PDF.\n\nconst doc = Api.GetDocument();\nconst page = doc.GetPage(0);\n\nconst fill = Api.CreateSolidFill(Api.RGB(255, 111, 61));\nconst stroke = Api.CreateStroke(0, Api.CreateNoFill());\nconst shape = Api.CreateShape('rect', Api.MillimetersToEmus(300), Api.MillimetersToEmus(130), fill, stroke);\nshape.SetPosition(Api.MillimetersToEmus(20), Api.MillimetersToEmus(35));\nconst docContent = shape.GetContent();\nconst paragraph = docContent.GetElement(0);\nparagraph.AddText(\"This is a paragraph with the font family set to 'Consolas'.\");\nparagraph.SetFontFamily('Consolas');\npage.AddObject(shape);"
  ],
  "params": [
    {
      "description": "The font family or families used for the current paragraph.",
      "name": "sFontFamily",
      "type": "string"
    }
  ],
  "returns": {
    "description": "this",
    "type": "ApiParagraph"
  },
  "signature": "SetFontFamily(sFontFamily: string): ApiParagraph"
}
