{
  "description": "Sets the italic property to the text character.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/pdf-api/ApiParagraph/Methods/SetItalic/",
  "examples": [
    "// How do I apply italic formatting to text in a PDF?\n\n// Display text with a tilted appearance in a PDF.\n\nconst doc = Api.GetDocument();\nconst page = doc.GetPage(0);\n\nconst fill = Api.CreateSolidFill(Api.HexColor('#FF6F3D'));\nconst stroke = Api.CreateStroke(0, Api.CreateNoFill());\nconst shape = Api.CreateShape('roundRect', Api.MillimetersToEmus(300), Api.MillimetersToEmus(130), fill, stroke);\nshape.SetPosition(Api.MillimetersToEmus(20), Api.MillimetersToEmus(35));\nconst docContent = shape.GetContent();\nconst paragraph = docContent.GetElement(0);\nparagraph.AddText('This is a paragraph with the font set to italicized letters.');\nparagraph.SetItalic(true);\npage.AddObject(shape);"
  ],
  "params": [
    {
      "description": "Specifies that the contents of the current paragraph are displayed italicized.",
      "name": "isItalic",
      "type": "boolean"
    }
  ],
  "returns": {
    "description": "this",
    "type": "ApiParagraph"
  },
  "signature": "SetItalic(isItalic: boolean): ApiParagraph"
}
