{
  "description": "Creates a gradient stop used for different types of gradients.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/presentation-api/Api/Methods/CreateGradientStop/",
  "examples": [
    "// How do I create a gradient with multiple color points in a presentation?\n\n// Set color positions along a gradient fill to create smooth transitions in a presentation.\n\nconst presentation = Api.GetPresentation();\nconst slide = presentation.GetSlideByIndex(0);\nslide.RemoveAllObjects();\n\nconst gs1 = Api.CreateGradientStop(Api.RGB(255, 213, 191), 0);\nconst gs2 = Api.CreateGradientStop(Api.RGB(255, 111, 61), 100000);\nconst fill = Api.CreateRadialGradientFill([gs1, gs2]);\nconst stroke = Api.CreateStroke(0, Api.CreateNoFill());\nconst drawing = Api.CreateShape(\"flowChartMagneticTape\", 300 * 36000, 130 * 36000, fill, stroke);\ndrawing.SetPosition(608400, 1267200);\nslide.AddObject(drawing);"
  ],
  "params": [
    {
      "description": "The color used for the gradient stop.",
      "name": "color",
      "type": "ApiColor"
    },
    {
      "description": "The position of the gradient stop measured in 1000th of percent.",
      "name": "pos",
      "type": "PositivePercentage"
    }
  ],
  "returns": {
    "type": "ApiGradientStop"
  },
  "signature": "CreateGradientStop(color: ApiColor, pos: PositivePercentage): ApiGradientStop",
  "since": "9.1.0"
}
