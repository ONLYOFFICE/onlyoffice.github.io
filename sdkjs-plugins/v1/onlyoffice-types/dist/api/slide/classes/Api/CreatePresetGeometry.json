{
  "description": "Creates a geometry using one of the available preset shapes.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/presentation-api/Api/Methods/CreatePresetGeometry/",
  "examples": [
    "// How do I apply a preset shape to an object in a presentation?\n\n// Create a shape with a predefined geometry like a star in a presentation.\n\nlet presentation = Api.GetPresentation();\nlet slide = presentation.GetSlideByIndex(0);\n\n// Create preset geometry shape\nlet presetGeometry = Api.CreatePresetGeometry(\"star8\");\nlet fill = Api.CreateSolidFill(Api.RGB(255, 200, 0));\nlet stroke = Api.CreateStroke(36000, Api.CreateSolidFill(Api.RGB(200, 150, 0)));\nlet shape = Api.CreateShape(\"rect\", 80 * 36000, 80 * 36000, fill, stroke);\nshape.SetGeometry(presetGeometry);\nshape.SetPosition(1000000, 1000000);\nlet docContent = shape.GetDocContent();\nif (docContent)\n{\n    let paragraph = docContent.GetElement(0);\n    paragraph.AddText(\"Preset: \" + presetGeometry.GetPreset() + \" (Custom: \" + presetGeometry.IsCustom() + \")\");\n}\nslide.AddObject(shape);"
  ],
  "params": [
    {
      "description": "The preset name.",
      "name": "preset",
      "optional": true,
      "type": "ShapeType"
    }
  ],
  "returns": {
    "type": "ApiGeometry"
  },
  "signature": "CreatePresetGeometry(preset?: ShapeType): ApiGeometry",
  "since": "9.1.0"
}
