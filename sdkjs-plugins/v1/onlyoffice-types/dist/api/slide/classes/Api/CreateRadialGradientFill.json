{
  "description": "Creates a radial gradient fill to apply to the object using the selected radial gradient as the object background.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/presentation-api/Api/Methods/CreateRadialGradientFill/",
  "examples": [
    "// How do I apply a radial gradient to a shape in a presentation?\n\n// Create a gradient that fades from one color at the center to another at the edges in a presentation.\n\nconst presentation = Api.GetPresentation();\nconst slide = presentation.GetSlideByIndex(0);\nslide.RemoveAllObjects();\n\nconst gs1 = Api.CreateGradientStop(Api.CreatePresetColor(\"peachPuff\"), 0);\nconst gs2 = Api.CreateGradientStop(Api.RGB(255, 111, 61), 100000);\nconst fill = Api.CreateRadialGradientFill([gs1, gs2]);\nconst stroke = Api.CreateStroke(0, Api.CreateNoFill());\nconst drawing = Api.CreateShape(\"flowChartMagneticTape\", 300 * 36000, 130 * 36000, fill, stroke);\ndrawing.SetPosition(608400, 1267200);\nslide.AddObject(drawing);"
  ],
  "params": [
    {
      "description": "The array of gradient color stops measured in 1000th of percent.",
      "name": "gradientStops",
      "type": "number[]"
    }
  ],
  "returns": {
    "type": "ApiFill"
  },
  "signature": "CreateRadialGradientFill(gradientStops: number[]): ApiFill"
}
