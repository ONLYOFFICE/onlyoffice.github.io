{
  "description": "Creates a shape with the parameters specified.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/presentation-api/Api/Methods/CreateShape/",
  "examples": [
    "// How do I create a custom shape with colors in a presentation?\n\n// Set up a shape with a fill color, outline, and add text to it in a presentation.\n\nconst presentation = Api.GetPresentation();\nconst slide = presentation.GetSlideByIndex(0);\nslide.RemoveAllObjects();\n\nconst fill = Api.CreateSolidFill(Api.RGB(255, 111, 61));\nconst stroke = Api.CreateStroke(0, Api.CreateNoFill());\nconst shape = Api.CreateShape(\"flowChartMagneticTape\", 300 * 36000, 130 * 36000, fill, stroke);\nshape.SetPosition(608400, 1267200);\nconst docContent = shape.GetDocContent();\nconst paragraph = docContent.GetElement(0);\n\nlet run = Api.CreateRun();\nrun.SetFontSize(60);\nrun.AddText(\"This is just a sample text. \");\nparagraph.AddElement(run);\n\nrun = Api.CreateRun();\nrun.SetFontSize(60);\nrun.SetFontFamily(\"Comic Sans MS\");\nrun.AddText(\"This is a text run with the font family set to 'Comic Sans MS'.\");\nparagraph.AddElement(run);\nslide.AddObject(shape);"
  ],
  "params": [
    {
      "description": "The shape type which specifies the preset shape geometry.",
      "name": "sType",
      "optional": true,
      "type": "ShapeType"
    },
    {
      "description": "The shape width in English measure units.",
      "name": "nWidth",
      "optional": true,
      "type": "EMU"
    },
    {
      "description": "The shape height in English measure units.",
      "name": "nHeight",
      "optional": true,
      "type": "EMU"
    },
    {
      "description": "The color or pattern used to fill the shape. If not specified, the default shape style fill (theme accent) is used.",
      "name": "oFill",
      "optional": true,
      "type": "ApiFill"
    },
    {
      "description": "The stroke used to draw the shape outline. If not specified, the default shape style outline (theme accent) is used.",
      "name": "oStroke",
      "optional": true,
      "type": "ApiStroke"
    }
  ],
  "returns": {
    "type": "ApiShape"
  },
  "signature": "CreateShape(sType?: ShapeType, nWidth?: EMU, nHeight?: EMU, oFill?: ApiFill, oStroke?: ApiStroke): ApiShape"
}
