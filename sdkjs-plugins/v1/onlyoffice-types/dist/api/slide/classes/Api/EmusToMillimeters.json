{
  "description": "Converts English Metric Units (EMUs) to millimeters.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/presentation-api/Api/Methods/EmusToMillimeters/",
  "examples": [
    "// How can I convert between emus and millimeters for object sizing in a presentation?\n\n// Transform measurement values to display size equivalents in a presentation.\n\nconst emus = 780000;\nconst millimeters = Api.EmusToMillimeters(emus);\n\nconst rgb = Api.CreateRGBColor(50, 150, 250);\nconst fill = Api.CreateSolidFill(rgb);\nconst stroke = Api.CreateStroke(0, Api.CreateNoFill());\nconst shape = Api.CreateShape('rect', 300 * 36000, 150 * 36000, fill, stroke);\nconst paragraph = shape.GetContent().GetElement(0);\nparagraph.AddText(emus + ' emus are equal to ' + millimeters + ' millimeters.');\n\nconst presentation = Api.GetPresentation();\nconst slide = presentation.GetSlideByIndex(0);\nslide.RemoveAllObjects();\nslide.AddObject(shape);"
  ],
  "params": [
    {
      "description": "The value in English Metric Units (EMUs).",
      "name": "emu",
      "type": "EMU"
    }
  ],
  "returns": {
    "description": "The value in millimeters.",
    "type": "mm"
  },
  "signature": "EmusToMillimeters(emu: EMU): mm"
}
