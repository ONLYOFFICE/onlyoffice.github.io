{
  "description": "Converts EMUs (English Metric Units) to points.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/presentation-api/Api/Methods/EmusToPoints/",
  "examples": [
    "// How can I convert emus to points for font and size measurements in a presentation?\n\n// Transform measurement values to display point equivalents in a presentation.\n\nconst emus = 100;\nconst points = Api.EmusToPoints(emus);\n\nconst rgb = Api.RGB(50, 150, 250);\nconst fill = Api.CreateSolidFill(rgb);\nconst stroke = Api.CreateStroke(0, Api.CreateNoFill());\nconst shape = Api.CreateShape('rect', 300 * 36000, 150 * 36000, fill, stroke);\nconst paragraph = shape.GetContent().GetElement(0);\nparagraph.AddText(emus + ' emus are equal to ' + points + ' points.');\n\nconst presentation = Api.GetPresentation();\nconst slide = presentation.GetSlideByIndex(0);\nslide.RemoveAllObjects();\nslide.AddObject(shape);"
  ],
  "params": [
    {
      "description": "The number of EMUs to convert to points.",
      "name": "emu",
      "type": "number"
    }
  ],
  "returns": {
    "type": "number"
  },
  "signature": "EmusToPoints(emu: number): number"
}
