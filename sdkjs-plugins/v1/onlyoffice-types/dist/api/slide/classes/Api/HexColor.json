{
  "description": "Creates a color from a HEX string.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/presentation-api/Api/Methods/HexColor/",
  "examples": [
    "// Create a hex color using the presentation API.\n\n// Apply the hex color to a shape fill.\n\nconst presentation = Api.GetPresentation();\nconst slide = presentation.GetSlideByIndex(0);\nslide.RemoveAllObjects();\n\nconst color = Api.HexColor('#DECADE');\nconst fill = Api.CreateSolidFill(color);\nconst stroke = Api.CreateStroke(36000, Api.CreateSolidFill(Api.RGB(0, 0, 0)));\nconst shape = Api.CreateShape(\"rect\", 300 * 36000, 130 * 36000, fill, stroke);\nshape.SetPosition(608400, 1267200);\nslide.AddObject(shape);"
  ],
  "params": [
    {
      "description": "The HEX string representing a color.",
      "name": "hexString",
      "type": "string"
    }
  ],
  "returns": {
    "type": "ApiColor"
  },
  "signature": "HexColor(hexString: string): ApiColor"
}
