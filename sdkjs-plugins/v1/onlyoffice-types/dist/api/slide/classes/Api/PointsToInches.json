{
  "description": "Converts points to inches.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/presentation-api/Api/Methods/PointsToInches/",
  "examples": [
    "// Use Api.PointsToInches to convert measurement units.\n\n// Display the converted value in a shape text.\n\nconst points = 1000;\nconst inches = Api.PointsToInches(points);\n\nconst rgb = Api.RGB(50, 150, 250);\nconst fill = Api.CreateSolidFill(rgb);\nconst stroke = Api.CreateStroke(0, Api.CreateNoFill());\nconst shape = Api.CreateShape('rect', 300 * 36000, 150 * 36000, fill, stroke);\nconst paragraph = shape.GetContent().GetElement(0);\nparagraph.AddText(points + ' points are equal to ' + inches + ' inches.');\n\nconst presentation = Api.GetPresentation();\nconst slide = presentation.GetSlideByIndex(0);\nslide.RemoveAllObjects();\nslide.AddObject(shape);"
  ],
  "params": [
    {
      "description": "The number of points to convert to inches.",
      "name": "pt",
      "type": "number"
    }
  ],
  "returns": {
    "type": "number"
  },
  "signature": "PointsToInches(pt: number): number"
}
