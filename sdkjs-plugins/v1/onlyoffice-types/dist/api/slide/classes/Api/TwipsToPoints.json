{
  "description": "Converts twips to points.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/presentation-api/Api/Methods/TwipsToPoints/",
  "examples": [
    "// Twips are units of measurement where 1440 twips equals 1 inch.\n\n// Use Api.TwipsToPoints() to convert measurement units for text sizing.\n\nconst twips = 100;\nconst points = Api.TwipsToPoints(twips);\n\nconst rgb = Api.RGB(50, 150, 250);\nconst fill = Api.CreateSolidFill(rgb);\nconst stroke = Api.CreateStroke(0, Api.CreateNoFill());\nconst shape = Api.CreateShape('rect', 300 * 36000, 150 * 36000, fill, stroke);\nconst paragraph = shape.GetContent().GetElement(0);\nparagraph.AddText(twips + ' twips are equal to ' + points + ' points.');\n\nconst presentation = Api.GetPresentation();\nconst slide = presentation.GetSlideByIndex(0);\nslide.RemoveAllObjects();\nslide.AddObject(shape);"
  ],
  "params": [
    {
      "description": "The number of twips to convert to points.",
      "name": "twips",
      "type": "number"
    }
  ],
  "returns": {
    "type": "number"
  },
  "signature": "TwipsToPoints(twips: number): number"
}
