{
  "description": "Class representing an animation effect.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/presentation-api/ApiAnimationEffect/",
  "methods": {
    "Delete": {
      "description": "Deletes the animation effect.",
      "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/presentation-api/ApiAnimationEffect/Methods/Delete/",
      "examples": [
        "// Removing animation effects modifies the sequence of object animations on slides.\n\n// Use the Delete() method to remove an effect and verify the count change.\n\nconst presentation = Api.GetPresentation();\nconst slide = presentation.GetSlideByIndex(0);\n\nconst shape = Api.CreateShape(\n\t'rect',\n\t150 * 36000, 100 * 36000,\n\tApi.CreateSolidFill(Api.CreateRGBColor(61, 74, 107)),\n\tApi.CreateStroke(0, Api.CreateNoFill())\n);\nshape.SetPosition(100 * 36000, 80 * 36000);\nslide.AddObject(shape);\n\nconst timeLine = slide.GetTimeLine();\nconst mainSequence = timeLine.GetMainSequence();\nconst effect = mainSequence.AddEffect(shape, 'entranceFade', 'onclick');\n\nconst countBefore = mainSequence.GetCount();\n\n// Delete the effect\neffect.Delete();\n\nconst countAfter = mainSequence.GetCount();\n\nconst infoShape = Api.CreateShape(\n\t'rect',\n\t150 * 36000, 40 * 36000,\n\tApi.CreateSolidFill(Api.CreateRGBColor(200, 200, 200)),\n\tApi.CreateStroke(0, Api.CreateNoFill())\n);\ninfoShape.SetPosition(20 * 36000, 10 * 36000);\nconst content = infoShape.GetDocContent();\nconst paragraph = content.GetElement(0);\nparagraph.AddText('Effects before delete: ' + countBefore);\nparagraph.AddLineBreak();\nparagraph.AddText('Effects after delete: ' + countAfter);\nslide.AddObject(infoShape);"
      ],
      "params": [],
      "returns": {
        "description": "True if the effect was deleted successfully.",
        "type": "boolean"
      },
      "signature": "Delete(): boolean",
      "since": "9.3.0"
    },
    "GetClassType": {
      "description": "Returns the type of the ApiAnimationEffect class.",
      "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/presentation-api/ApiAnimationEffect/Methods/GetClassType/",
      "examples": [
        "// Class type identifiers distinguish different object types in the animation system.\n\n// Call GetClassType() on an animation effect object and display the result.\n\nconst presentation = Api.GetPresentation();\nconst slide = presentation.GetSlideByIndex(0);\n\nconst shape = Api.CreateShape(\n\t'rect',\n\t150 * 36000, 100 * 36000,\n\tApi.CreateSolidFill(Api.CreateRGBColor(61, 74, 107)),\n\tApi.CreateStroke(0, Api.CreateNoFill())\n);\nshape.SetPosition(100 * 36000, 80 * 36000);\nslide.AddObject(shape);\n\nconst timeLine = slide.GetTimeLine();\nconst mainSequence = timeLine.GetMainSequence();\nconst effect = mainSequence.AddEffect(shape, 'entranceFade', 'onclick');\n\nconst classType = effect.GetClassType();\n\nconst infoShape = Api.CreateShape(\n\t'rect',\n\t150 * 36000, 40 * 36000,\n\tApi.CreateSolidFill(Api.CreateRGBColor(200, 200, 200)),\n\tApi.CreateStroke(0, Api.CreateNoFill())\n);\ninfoShape.SetPosition(20 * 36000, 10 * 36000);\nconst content = infoShape.GetDocContent();\nconst paragraph = content.GetElement(0);\nparagraph.AddText('Class type: ' + classType);\nslide.AddObject(infoShape);"
      ],
      "params": [],
      "returns": {
        "type": "\"animationEffect\""
      },
      "signature": "GetClassType(): \"animationEffect\"",
      "since": "9.3.0"
    },
    "GetDelay": {
      "description": "Returns the delay before the animation effect starts in milliseconds.",
      "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/presentation-api/ApiAnimationEffect/Methods/GetDelay/",
      "examples": [
        "// Delay values control the timing offset before an animation starts.\n\n// Retrieve the delay from an animation effect using GetDelay() and display it.\n\nconst presentation = Api.GetPresentation();\nconst slide = presentation.GetSlideByIndex(0);\n\nconst shape = Api.CreateShape(\n\t'rect',\n\t150 * 36000, 100 * 36000,\n\tApi.CreateSolidFill(Api.CreateRGBColor(61, 74, 107)),\n\tApi.CreateStroke(0, Api.CreateNoFill())\n);\nshape.SetPosition(100 * 36000, 80 * 36000);\nslide.AddObject(shape);\n\nconst timeLine = slide.GetTimeLine();\nconst mainSequence = timeLine.GetMainSequence();\nconst effect = mainSequence.AddEffect(shape, 'entranceFade', 'onclick');\n\nconst delay = effect.GetDelay();\n\nconst infoShape = Api.CreateShape(\n\t'rect',\n\t150 * 36000, 40 * 36000,\n\tApi.CreateSolidFill(Api.CreateRGBColor(200, 200, 200)),\n\tApi.CreateStroke(0, Api.CreateNoFill())\n);\ninfoShape.SetPosition(20 * 36000, 10 * 36000);\nconst content = infoShape.GetDocContent();\nconst paragraph = content.GetElement(0);\nparagraph.AddText('Delay: ' + delay + 'ms');\nslide.AddObject(infoShape);"
      ],
      "params": [],
      "returns": {
        "description": "The delay in milliseconds.",
        "type": "number"
      },
      "signature": "GetDelay(): number",
      "since": "9.3.0"
    },
    "GetDuration": {
      "description": "Returns the duration of the animation effect in milliseconds.",
      "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/presentation-api/ApiAnimationEffect/Methods/GetDuration/",
      "examples": [
        "// Duration values specify how long an animation takes to complete.\n\n// Retrieve the duration from an animation effect using GetDuration() and display it.\n\nconst presentation = Api.GetPresentation();\nconst slide = presentation.GetSlideByIndex(0);\n\nconst shape = Api.CreateShape(\n\t'rect',\n\t150 * 36000, 100 * 36000,\n\tApi.CreateSolidFill(Api.CreateRGBColor(61, 74, 107)),\n\tApi.CreateStroke(0, Api.CreateNoFill())\n);\nshape.SetPosition(100 * 36000, 80 * 36000);\nslide.AddObject(shape);\n\nconst timeLine = slide.GetTimeLine();\nconst mainSequence = timeLine.GetMainSequence();\nconst effect = mainSequence.AddEffect(shape, 'entranceFade', 'onclick');\n\nconst duration = effect.GetDuration();\n\nconst infoShape = Api.CreateShape(\n\t'rect',\n\t150 * 36000, 40 * 36000,\n\tApi.CreateSolidFill(Api.CreateRGBColor(200, 200, 200)),\n\tApi.CreateStroke(0, Api.CreateNoFill())\n);\ninfoShape.SetPosition(20 * 36000, 10 * 36000);\nconst content = infoShape.GetDocContent();\nconst paragraph = content.GetElement(0);\nparagraph.AddText('Duration: ' + duration + 'ms');\nslide.AddObject(infoShape);"
      ],
      "params": [],
      "returns": {
        "description": "The duration in milliseconds.",
        "type": "number"
      },
      "signature": "GetDuration(): number",
      "since": "9.3.0"
    },
    "GetEffectType": {
      "description": "Returns the animation effect type.",
      "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/presentation-api/ApiAnimationEffect/Methods/GetEffectType/",
      "examples": [
        "// Effect types categorize animations (entrance, emphasis, exit, motion paths).\n\n// Retrieve the effect type using GetEffectType() and display the result.\n\nconst presentation = Api.GetPresentation();\nconst slide = presentation.GetSlideByIndex(0);\n\nconst shape = Api.CreateShape(\n\t'rect',\n\t150 * 36000, 100 * 36000,\n\tApi.CreateSolidFill(Api.CreateRGBColor(61, 74, 107)),\n\tApi.CreateStroke(0, Api.CreateNoFill())\n);\nshape.SetPosition(100 * 36000, 80 * 36000);\nslide.AddObject(shape);\n\nconst timeLine = slide.GetTimeLine();\nconst mainSequence = timeLine.GetMainSequence();\nconst effect = mainSequence.AddEffect(shape, 'entranceFlyIn', 'onclick');\n\nconst effectType = effect.GetEffectType();\n\nconst infoShape = Api.CreateShape(\n\t'rect',\n\t150 * 36000, 40 * 36000,\n\tApi.CreateSolidFill(Api.CreateRGBColor(200, 200, 200)),\n\tApi.CreateStroke(0, Api.CreateNoFill())\n);\ninfoShape.SetPosition(20 * 36000, 10 * 36000);\nconst content = infoShape.GetDocContent();\nconst paragraph = content.GetElement(0);\nparagraph.AddText('Effect type: ' + effectType);\nslide.AddObject(infoShape);"
      ],
      "params": [],
      "returns": {
        "description": "The effect type name (e.g., \"entranceFade\", \"exitFadeOut\", \"emphasisPulse\"), or null if unknown.",
        "type": "AnimationEffectType | null"
      },
      "signature": "GetEffectType(): AnimationEffectType | null",
      "since": "9.3.0"
    },
    "GetRepeatCount": {
      "description": "Returns the repeat count for the animation effect.",
      "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/presentation-api/ApiAnimationEffect/Methods/GetRepeatCount/",
      "examples": [
        "// Repeat count determines how many times an animation plays consecutively.\n\n// Retrieve the repeat count from an animation effect using GetRepeatCount() and display it.\n\nconst presentation = Api.GetPresentation();\nconst slide = presentation.GetSlideByIndex(0);\n\nconst shape = Api.CreateShape(\n\t'rect',\n\t150 * 36000, 100 * 36000,\n\tApi.CreateSolidFill(Api.CreateRGBColor(61, 74, 107)),\n\tApi.CreateStroke(0, Api.CreateNoFill())\n);\nshape.SetPosition(100 * 36000, 80 * 36000);\nslide.AddObject(shape);\n\nconst timeLine = slide.GetTimeLine();\nconst mainSequence = timeLine.GetMainSequence();\nconst effect = mainSequence.AddEffect(shape, 'emphasisPulse', 'onclick');\n\nconst repeatCount = effect.GetRepeatCount();\n\nconst infoShape = Api.CreateShape(\n\t'rect',\n\t150 * 36000, 40 * 36000,\n\tApi.CreateSolidFill(Api.CreateRGBColor(200, 200, 200)),\n\tApi.CreateStroke(0, Api.CreateNoFill())\n);\ninfoShape.SetPosition(20 * 36000, 10 * 36000);\nconst content = infoShape.GetDocContent();\nconst paragraph = content.GetElement(0);\nparagraph.AddText('Repeat count: ' + repeatCount);\nslide.AddObject(infoShape);"
      ],
      "params": [],
      "returns": {
        "description": "The repeat count (1 = play once, 2 = play twice, etc.). Returns 1 if not set.",
        "type": "number"
      },
      "signature": "GetRepeatCount(): number",
      "since": "9.3.0"
    },
    "GetShape": {
      "description": "Returns the animated drawing object.",
      "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/presentation-api/ApiAnimationEffect/Methods/GetShape/",
      "examples": [
        "// Each animation effect is bound to a specific shape on the slide.\n\n// Retrieve the shape from an animation effect using GetShape() and display its type.\n\nconst presentation = Api.GetPresentation();\nconst slide = presentation.GetSlideByIndex(0);\n\nconst shape = Api.CreateShape(\n\t'rect',\n\t150 * 36000, 100 * 36000,\n\tApi.CreateSolidFill(Api.CreateRGBColor(61, 74, 107)),\n\tApi.CreateStroke(0, Api.CreateNoFill())\n);\nshape.SetPosition(100 * 36000, 80 * 36000);\nslide.AddObject(shape);\n\nconst timeLine = slide.GetTimeLine();\nconst mainSequence = timeLine.GetMainSequence();\nconst effect = mainSequence.AddEffect(shape, 'entranceFade', 'onclick');\n\nconst animatedShape = effect.GetShape();\nconst shapeType = animatedShape ? animatedShape.GetClassType() : 'none';\n\nconst infoShape = Api.CreateShape(\n\t'rect',\n\t150 * 36000, 40 * 36000,\n\tApi.CreateSolidFill(Api.CreateRGBColor(200, 200, 200)),\n\tApi.CreateStroke(0, Api.CreateNoFill())\n);\ninfoShape.SetPosition(20 * 36000, 10 * 36000);\nconst content = infoShape.GetDocContent();\nconst paragraph = content.GetElement(0);\nparagraph.AddText('Animated object type: ' + shapeType);\nslide.AddObject(infoShape);"
      ],
      "params": [],
      "returns": {
        "description": "The animated drawing or null if not found.",
        "type": "ApiDrawing | null"
      },
      "signature": "GetShape(): ApiDrawing | null",
      "since": "9.3.0"
    },
    "GetTriggerType": {
      "description": "Returns the trigger type for the animation effect.",
      "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/presentation-api/ApiAnimationEffect/Methods/GetTriggerType/",
      "examples": [
        "// How do I read the trigger type of an animation effect in a presentation?\n\n// Retrieve the setting that controls when an animation plays in a presentation.\n\nconst presentation = Api.GetPresentation();\nconst slide = presentation.GetSlideByIndex(0);\n\nconst shape = Api.CreateShape(\n\t'rect',\n\t150 * 36000, 100 * 36000,\n\tApi.CreateSolidFill(Api.CreateRGBColor(61, 74, 107)),\n\tApi.CreateStroke(0, Api.CreateNoFill())\n);\nshape.SetPosition(100 * 36000, 80 * 36000);\nslide.AddObject(shape);\n\nconst timeLine = slide.GetTimeLine();\nconst mainSequence = timeLine.GetMainSequence();\nconst effect = mainSequence.AddEffect(shape, 'entranceFade', 'afterprevious');\n\nconst triggerType = effect.GetTriggerType();\n\nconst infoShape = Api.CreateShape(\n\t'rect',\n\t150 * 36000, 40 * 36000,\n\tApi.CreateSolidFill(Api.CreateRGBColor(200, 200, 200)),\n\tApi.CreateStroke(0, Api.CreateNoFill())\n);\ninfoShape.SetPosition(20 * 36000, 10 * 36000);\nconst content = infoShape.GetDocContent();\nconst paragraph = content.GetElement(0);\nparagraph.AddText('Trigger type: ' + triggerType);\nslide.AddObject(infoShape);"
      ],
      "params": [],
      "returns": {
        "description": "The trigger type: \"onclick\", \"withprevious\", or \"afterprevious\".",
        "type": "AnimationTriggerType"
      },
      "signature": "GetTriggerType(): AnimationTriggerType",
      "since": "9.3.0"
    },
    "MoveTo": {
      "description": "Moves the animation effect to the specified position in the sequence.",
      "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/presentation-api/ApiAnimationEffect/Methods/MoveTo/",
      "examples": [
        "// How do I change the position of an animation in a list of animations in a presentation?\n\n// Rearrange when an animation plays relative to other animations in a presentation.\n\nconst presentation = Api.GetPresentation();\nconst slide = presentation.GetSlideByIndex(0);\n\n// Create shapes\nconst shape1 = Api.CreateShape(\n\t'rect',\n\t80 * 36000, 80 * 36000,\n\tApi.CreateSolidFill(Api.CreateRGBColor(61, 74, 107)),\n\tApi.CreateStroke(0, Api.CreateNoFill())\n);\nshape1.SetPosition(30 * 36000, 80 * 36000);\nslide.AddObject(shape1);\n\nconst shape2 = Api.CreateShape(\n\t'ellipse',\n\t80 * 36000, 80 * 36000,\n\tApi.CreateSolidFill(Api.CreateRGBColor(107, 61, 74)),\n\tApi.CreateStroke(0, Api.CreateNoFill())\n);\nshape2.SetPosition(130 * 36000, 80 * 36000);\nslide.AddObject(shape2);\n\nconst shape3 = Api.CreateShape(\n\t'star5',\n\t80 * 36000, 80 * 36000,\n\tApi.CreateSolidFill(Api.CreateRGBColor(74, 107, 61)),\n\tApi.CreateStroke(0, Api.CreateNoFill())\n);\nshape3.SetPosition(230 * 36000, 80 * 36000);\nslide.AddObject(shape3);\n\n// Add animations\nconst timeLine = slide.GetTimeLine();\nconst mainSequence = timeLine.GetMainSequence();\nconst effect1 = mainSequence.AddEffect(shape1, 'entranceFade', 'onclick');\nmainSequence.AddEffect(shape2, 'entranceFlyIn', 'afterprevious');\nmainSequence.AddEffect(shape3, 'entranceBounce', 'afterprevious');\n\n// Move first effect to position 2\neffect1.MoveTo(2);\n\nconst infoShape = Api.CreateShape(\n\t'rect',\n\t200 * 36000, 40 * 36000,\n\tApi.CreateSolidFill(Api.CreateRGBColor(200, 200, 200)),\n\tApi.CreateStroke(0, Api.CreateNoFill())\n);\ninfoShape.SetPosition(20 * 36000, 10 * 36000);\nconst content = infoShape.GetDocContent();\nconst paragraph = content.GetElement(0);\nparagraph.AddText('First effect moved to position 2');\nslide.AddObject(infoShape);"
      ],
      "params": [
        {
          "description": "The new zero-based position for the effect.",
          "name": "index",
          "type": "number"
        }
      ],
      "returns": {
        "description": "True if the effect was moved successfully.",
        "type": "boolean"
      },
      "signature": "MoveTo(index: number): boolean",
      "since": "9.3.0"
    },
    "SetDelay": {
      "description": "Sets the delay before the animation effect starts in milliseconds.",
      "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/presentation-api/ApiAnimationEffect/Methods/SetDelay/",
      "examples": [
        "// How do I add a pause before an animation plays in a presentation?\n\n// Configure the waiting time between the trigger and when the animation begins in a presentation.\n\nconst presentation = Api.GetPresentation();\nconst slide = presentation.GetSlideByIndex(0);\n\nconst shape = Api.CreateShape(\n\t'rect',\n\t150 * 36000, 100 * 36000,\n\tApi.CreateSolidFill(Api.CreateRGBColor(61, 74, 107)),\n\tApi.CreateStroke(0, Api.CreateNoFill())\n);\nshape.SetPosition(100 * 36000, 80 * 36000);\nslide.AddObject(shape);\n\nconst timeLine = slide.GetTimeLine();\nconst mainSequence = timeLine.GetMainSequence();\nconst effect = mainSequence.AddEffect(shape, 'entranceFade', 'onclick');\n\n// Set 1 second delay\neffect.SetDelay(1000);\nconst delay = effect.GetDelay();\n\nconst infoShape = Api.CreateShape(\n\t'rect',\n\t150 * 36000, 40 * 36000,\n\tApi.CreateSolidFill(Api.CreateRGBColor(200, 200, 200)),\n\tApi.CreateStroke(0, Api.CreateNoFill())\n);\ninfoShape.SetPosition(20 * 36000, 10 * 36000);\nconst content = infoShape.GetDocContent();\nconst paragraph = content.GetElement(0);\nparagraph.AddText('New delay: ' + delay + 'ms (1 second)');\nslide.AddObject(infoShape);"
      ],
      "params": [
        {
          "description": "The delay in milliseconds.",
          "name": "delay",
          "type": "number"
        }
      ],
      "returns": {
        "description": "True if the delay was set successfully.",
        "type": "boolean"
      },
      "signature": "SetDelay(delay: number): boolean",
      "since": "9.3.0"
    },
    "SetDuration": {
      "description": "Sets the duration of the animation effect in milliseconds.",
      "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/presentation-api/ApiAnimationEffect/Methods/SetDuration/",
      "examples": [
        "// How do I control the speed of an animation in a presentation?\n\n// Adjust the time span for an animation to run from start to finish in a presentation.\n\nconst presentation = Api.GetPresentation();\nconst slide = presentation.GetSlideByIndex(0);\n\nconst shape = Api.CreateShape(\n\t'rect',\n\t150 * 36000, 100 * 36000,\n\tApi.CreateSolidFill(Api.CreateRGBColor(61, 74, 107)),\n\tApi.CreateStroke(0, Api.CreateNoFill())\n);\nshape.SetPosition(100 * 36000, 80 * 36000);\nslide.AddObject(shape);\n\nconst timeLine = slide.GetTimeLine();\nconst mainSequence = timeLine.GetMainSequence();\nconst effect = mainSequence.AddEffect(shape, 'entranceFade', 'onclick');\n\n// Set duration to 2 seconds\neffect.SetDuration(2000);\nconst duration = effect.GetDuration();\n\nconst infoShape = Api.CreateShape(\n\t'rect',\n\t150 * 36000, 40 * 36000,\n\tApi.CreateSolidFill(Api.CreateRGBColor(200, 200, 200)),\n\tApi.CreateStroke(0, Api.CreateNoFill())\n);\ninfoShape.SetPosition(20 * 36000, 10 * 36000);\nconst content = infoShape.GetDocContent();\nconst paragraph = content.GetElement(0);\nparagraph.AddText('New duration: ' + duration + 'ms (2 seconds)');\nslide.AddObject(infoShape);"
      ],
      "params": [
        {
          "description": "The duration in milliseconds.",
          "name": "duration",
          "type": "number"
        }
      ],
      "returns": {
        "description": "True if the duration was set successfully.",
        "type": "boolean"
      },
      "signature": "SetDuration(duration: number): boolean",
      "since": "9.3.0"
    },
    "SetRepeatCount": {
      "description": "Sets the repeat count for the animation effect.",
      "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/presentation-api/ApiAnimationEffect/Methods/SetRepeatCount/",
      "examples": [
        "// How do I make an animation play multiple times in a presentation?\n\n// Configure an animation to loop a specified number of times in a presentation.\n\nconst presentation = Api.GetPresentation();\nconst slide = presentation.GetSlideByIndex(0);\n\nconst shape = Api.CreateShape(\n\t'rect',\n\t150 * 36000, 100 * 36000,\n\tApi.CreateSolidFill(Api.CreateRGBColor(61, 74, 107)),\n\tApi.CreateStroke(0, Api.CreateNoFill())\n);\nshape.SetPosition(100 * 36000, 80 * 36000);\nslide.AddObject(shape);\n\nconst timeLine = slide.GetTimeLine();\nconst mainSequence = timeLine.GetMainSequence();\nconst effect = mainSequence.AddEffect(shape, 'emphasisPulse', 'onclick');\n\n// Make it repeat 3 times\neffect.SetRepeatCount(3);\nconst repeatCount = effect.GetRepeatCount();\n\nconst infoShape = Api.CreateShape(\n\t'rect',\n\t150 * 36000, 40 * 36000,\n\tApi.CreateSolidFill(Api.CreateRGBColor(200, 200, 200)),\n\tApi.CreateStroke(0, Api.CreateNoFill())\n);\ninfoShape.SetPosition(20 * 36000, 10 * 36000);\nconst content = infoShape.GetDocContent();\nconst paragraph = content.GetElement(0);\nparagraph.AddText('New repeat count: ' + repeatCount);\nslide.AddObject(infoShape);"
      ],
      "params": [
        {
          "description": "The repeat count (1 = play once, 2 = play twice, etc.).",
          "name": "count",
          "type": "number"
        }
      ],
      "returns": {
        "description": "True if the repeat count was set successfully.",
        "type": "boolean"
      },
      "signature": "SetRepeatCount(count: number): boolean",
      "since": "9.3.0"
    },
    "SetTriggerType": {
      "description": "Sets the trigger type for the animation effect.",
      "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/presentation-api/ApiAnimationEffect/Methods/SetTriggerType/",
      "examples": [
        "// How do I control when an animation starts in a presentation?\n\n// Choose whether an animation plays on click, automatically, or with the previous animation in a presentation.\n\nconst presentation = Api.GetPresentation();\nconst slide = presentation.GetSlideByIndex(0);\n\nconst shape = Api.CreateShape(\n\t'rect',\n\t150 * 36000, 100 * 36000,\n\tApi.CreateSolidFill(Api.CreateRGBColor(61, 74, 107)),\n\tApi.CreateStroke(0, Api.CreateNoFill())\n);\nshape.SetPosition(100 * 36000, 80 * 36000);\nslide.AddObject(shape);\n\nconst timeLine = slide.GetTimeLine();\nconst mainSequence = timeLine.GetMainSequence();\nconst effect = mainSequence.AddEffect(shape, 'entranceFade', 'onclick');\n\n// Change trigger type\neffect.SetTriggerType('withprevious');\nconst newTriggerType = effect.GetTriggerType();\n\nconst infoShape = Api.CreateShape(\n\t'rect',\n\t150 * 36000, 40 * 36000,\n\tApi.CreateSolidFill(Api.CreateRGBColor(200, 200, 200)),\n\tApi.CreateStroke(0, Api.CreateNoFill())\n);\ninfoShape.SetPosition(20 * 36000, 10 * 36000);\nconst content = infoShape.GetDocContent();\nconst paragraph = content.GetElement(0);\nparagraph.AddText('New trigger type: ' + newTriggerType);\nslide.AddObject(infoShape);"
      ],
      "params": [
        {
          "description": "The trigger type: \"onclick\", \"withprevious\", or \"afterprevious\".",
          "name": "trigger",
          "type": "AnimationTriggerType"
        }
      ],
      "returns": {
        "description": "True if the trigger type was set successfully.",
        "type": "boolean"
      },
      "signature": "SetTriggerType(trigger: AnimationTriggerType): boolean",
      "since": "9.3.0"
    }
  }
}
