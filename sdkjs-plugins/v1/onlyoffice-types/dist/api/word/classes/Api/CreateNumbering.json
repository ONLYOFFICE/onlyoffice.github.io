{
  "description": "Creates an abstract multilevel numbering with a specified type.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/document-api/Api/Methods/CreateNumbering/",
  "examples": [
    "// How do I create a bulleted list with nested indent levels in a document?\n\n// Format several paragraphs as a structured bullet list with increasing indentation in a document.\n\nlet doc = Api.GetDocument();\nlet numbering = doc.CreateNumbering(\"bullet\");\nfor (let lvl = 0; lvl < 2; ++lvl) \n{\n\tlet paragraph = Api.CreateParagraph();\n\tparagraph.AddText(\"This is an example of the bulleted paragraph № \" + (lvl + 1));\n\tparagraph.SetNumbering(numbering.GetLevel(lvl));\n\tdoc.Push(paragraph);\n}"
  ],
  "params": [
    {
      "description": "The type of the numbering which will be created.",
      "name": "sType",
      "optional": true,
      "type": "\"bullet\" | \"numbered\""
    }
  ],
  "returns": {
    "type": "ApiNumbering"
  },
  "signature": "CreateNumbering(sType?: \"bullet\" | \"numbered\"): ApiNumbering",
  "since": "9.4.0"
}
