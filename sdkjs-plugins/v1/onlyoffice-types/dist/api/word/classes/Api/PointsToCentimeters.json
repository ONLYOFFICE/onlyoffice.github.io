{
  "description": "Converts points to centimeters.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/document-api/Api/Methods/PointsToCentimeters/",
  "examples": [
    "// How do I find the centimeter equivalent of a measurement given in points in a document?\n\n// Write out how many centimeters correspond to a specific number of points in a document.\n\nconst doc = Api.GetDocument();\nconst paragraph = Api.CreateParagraph();\n\nconst points = 1000;\nconst centimeters = Api.PointsToCentimeters(points);\nparagraph.AddText(points + ' points are equal to ' + centimeters + ' centimeters.');\ndoc.Push(paragraph);"
  ],
  "params": [
    {
      "description": "The number of points to convert to centimeters.",
      "name": "pt",
      "type": "number"
    }
  ],
  "returns": {
    "type": "number"
  },
  "signature": "PointsToCentimeters(pt: number): number"
}
