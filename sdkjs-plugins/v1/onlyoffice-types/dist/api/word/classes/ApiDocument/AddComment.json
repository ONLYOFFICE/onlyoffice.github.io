{
  "description": "Adds a comment to the current document selection, or to the current word if no text is selected.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/document-api/ApiDocument/Methods/AddComment/",
  "examples": [
    "// How do I add an authored comment to a document?\n\n// Annotate a document with a reviewer's note tied to a specific author in a document.\n\nlet doc = Api.GetDocument(); \nlet paragraph = doc.GetElement(0); \nparagraph.AddText(\"This is just a sample text.\");\nparagraph.AddLineBreak();\nparagraph.AddText(\"The comment was added to this document.\");\ndoc.AddComment(\"This is a comment to the document.\", \"Jane\");"
  ],
  "params": [
    {
      "description": "The comment text.",
      "name": "sText",
      "type": "string"
    },
    {
      "description": "The author's name.",
      "name": "sAuthor",
      "optional": true,
      "type": "string"
    },
    {
      "description": "The user ID of the comment author.",
      "name": "sUserId",
      "optional": true,
      "type": "string"
    }
  ],
  "returns": {
    "description": "Returns null if the comment was not added.",
    "type": "ApiComment"
  },
  "signature": "AddComment(sText: string, sAuthor?: string, sUserId?: string): ApiComment"
}
