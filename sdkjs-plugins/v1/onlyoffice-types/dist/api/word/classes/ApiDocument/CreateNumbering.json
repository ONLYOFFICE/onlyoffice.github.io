{
  "description": "Creates an abstract multilevel numbering with a specified type.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/document-api/ApiDocument/Methods/CreateNumbering/",
  "examples": [
    "let doc = Api.GetDocument();\nlet numbering = doc.CreateNumbering(\"bullet\");\nfor (let lvl = 0; lvl < 8; ++lvl) {\n\tlet numLvl = numbering.GetLevel(lvl);\n\tlet paragraph = Api.CreateParagraph();\n\tparagraph.AddText(\"Default numbered lvl \" + (lvl + 1));\n\tparagraph.SetNumbering(numLvl);\n\tparagraph.SetContextualSpacing(true);\n\tdoc.Push(paragraph);\n}"
  ],
  "params": [
    {
      "description": "The type of the numbering which will be created.",
      "name": "sType",
      "optional": true,
      "type": "\"bullet\" | \"numbered\""
    }
  ],
  "returns": {
    "type": "ApiNumbering"
  },
  "signature": "CreateNumbering(sType?: \"bullet\" | \"numbered\"): ApiNumbering"
}
