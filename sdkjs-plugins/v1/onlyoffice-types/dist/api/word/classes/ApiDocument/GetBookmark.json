{
  "description": "Returns a bookmark by its name from the current document.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/document-api/ApiDocument/Methods/GetBookmark/",
  "examples": [
    "// How do I find a bookmark by name in a document?\n\n// Look up a bookmark to read the text it covers in a document.\n\nlet doc = Api.GetDocument();\nlet paragraph = doc.GetElement(0);\nparagraph.AddText(\"ONLYOFFICE Document Builder\");\nlet range1 = doc.GetRange(0, 11);\nrange1.AddBookmark(\"Bookmark_ex\");\nlet bookmark = doc.GetBookmark(\"Bookmark_ex\");\nparagraph.AddLineBreak();\nparagraph.AddText(\"Bookmark with name '\" + bookmark.GetName() + \"' surrounds text: \" + bookmark.GetText());"
  ],
  "params": [
    {
      "description": "The bookmark name.",
      "name": "sBookmarkName",
      "type": "string"
    }
  ],
  "returns": {
    "type": "ApiBookmark"
  },
  "signature": "GetBookmark(sBookmarkName: string): ApiBookmark",
  "since": "8.3.0"
}
