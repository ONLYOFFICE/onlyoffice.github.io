{
  "description": "Returns an element by its position in the document.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/document-api/ApiDocumentContent/Methods/GetElement/",
  "examples": [
    "let doc = Api.GetDocument();\nlet section = doc.GetFinalSection();\nlet docContent = section.GetHeader(\"default\", true);\nlet paragraph = docContent.GetElement(0);\nparagraph.AddText(\"This is the text in the default header\");"
  ],
  "params": [
    {
      "description": "The element position that will be taken from the document.",
      "name": "nPos",
      "type": "number"
    }
  ],
  "returns": {
    "type": "DocumentElement"
  },
  "signature": "GetElement(nPos: number): DocumentElement"
}
