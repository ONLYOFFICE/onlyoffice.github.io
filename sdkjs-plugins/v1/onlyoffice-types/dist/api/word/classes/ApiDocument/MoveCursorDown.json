{
  "description": "Moves the cursor down.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/document-api/ApiDocument/Methods/MoveCursorDown/",
  "examples": [
    "// How do I shift the cursor downward through several lines in a document?\n\n// Position the cursor at a lower line to read or insert content at that location in a document.\n\nlet doc = Api.GetDocument();\nconst paragraphCount = 5;\nfor (let i = 0; i < paragraphCount; i++) {\n    const newParagraph = Api.CreateParagraph();\n    newParagraph.AddText(\"This is \" + (i + 1) + \" paragraph.\");\n    doc.Push(newParagraph);\n}\n\ndoc.MoveCursorDown(3);\n\nlet paragraph = Api.CreateParagraph();\nparagraph.AddText(\"Current line is: \");\nparagraph.AddText(doc.GetCurrentSentence());\ndoc.Push(paragraph);"
  ],
  "params": [
    {
      "description": "Number of movements.",
      "name": "count",
      "optional": true,
      "type": "number"
    },
    {
      "description": "Specifies whether to select text during the move.",
      "name": "addToSelect",
      "optional": true,
      "type": "boolean"
    }
  ],
  "returns": {
    "type": "boolean"
  },
  "signature": "MoveCursorDown(count?: number, addToSelect?: boolean): boolean",
  "since": "9.2.0"
}
