{
  "description": "Sets the watermark settings in the current document.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/document-api/ApiDocument/Methods/SetWatermarkSettings/",
  "examples": [
    "let doc = Api.GetDocument();\nlet watermarkSettings = doc.GetWatermarkSettings();\nwatermarkSettings.SetType(\"text\");\nwatermarkSettings.SetText(\"Example\");\nlet textPr = watermarkSettings.GetTextPr();\ntextPr.SetFontFamily(\"Calibri\");\ntextPr.SetFontSize(-1);\ntextPr.SetDoubleStrikeout(true);\ntextPr.SetItalic(true);\ntextPr.SetBold(true);\ntextPr.SetUnderline(true);\ntextPr.SetColor(0, 255, 0);\ntextPr.SetHighlight(\"blue\");\nwatermarkSettings.SetTextPr(textPr);\ndoc.SetWatermarkSettings(watermarkSettings);"
  ],
  "params": [
    {
      "description": "The object which represents the watermark settings.",
      "name": "Settings",
      "type": "ApiWatermarkSettings"
    }
  ],
  "returns": {
    "description": "The object which represents the watermark drawing if the watermark type in Settings is not \"none\".",
    "type": "ApiDrawing"
  },
  "signature": "SetWatermarkSettings(Settings: ApiWatermarkSettings): ApiDrawing"
}
