{
  "description": "Adds a bookmark cross-reference to the current paragraph.\n<note>Please note that this paragraph must be in the document.</note>",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/document-api/ApiParagraph/Methods/AddBookmarkCrossRef/",
  "examples": [
    "let doc = Api.GetDocument(); \nlet paragraph = doc.GetElement(0); \nparagraph.AddText(\"ONLYOFFICE Document Builder\"); \nlet range = doc.GetRange(0, 9); \nrange.AddBookmark(\"Bookmark\");\nlet bookmarks = doc.GetAllBookmarksNames();\nparagraph = Api.CreateParagraph();\nparagraph.AddText(\"A bookmark was added \");\ndoc.Push(paragraph);\nparagraph.AddBookmarkCrossRef(\"aboveBelow\", bookmarks[0]);"
  ],
  "params": [
    {
      "description": "The text or numeric value of a bookmark reference you want to insert.",
      "name": "sRefTo",
      "type": "bookmarkRefTo"
    },
    {
      "description": "The name of the bookmark to be referred to (must be in the document).",
      "name": "sBookmarkName",
      "type": "string"
    },
    {
      "description": "Specifies if the reference will be inserted as a hyperlink.",
      "name": "bLink",
      "optional": true,
      "type": "boolean"
    },
    {
      "description": "Specifies if the above/below words indicating the position of the reference should be included (don't used with the \"text\" and \"aboveBelow\" sRefType).",
      "name": "bAboveBelow",
      "optional": true,
      "type": "boolean"
    },
    {
      "description": "A number separator (used only with the \"fullCtxParaNum\" sRefType).",
      "name": "sSepWith",
      "optional": true,
      "type": "string"
    }
  ],
  "returns": {
    "type": "boolean"
  },
  "signature": "AddBookmarkCrossRef(sRefTo: bookmarkRefTo, sBookmarkName: string, bLink?: boolean, bAboveBelow?: boolean, sSepWith?: string): boolean"
}
