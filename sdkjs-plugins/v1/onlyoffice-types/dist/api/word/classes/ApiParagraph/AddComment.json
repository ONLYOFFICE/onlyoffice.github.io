{
  "description": "Adds a comment to the current paragraph.\n<note>Please note that this paragraph must be in the document.</note>",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/document-api/ApiParagraph/Methods/AddComment/",
  "examples": [
    "// How do I add an author-attributed comment to a paragraph in a document?\n\n// Annotate a paragraph with feedback that identifies the commenter by name in a document.\n\nlet doc = Api.GetDocument();\nlet paragraph = doc.GetElement(0);\nparagraph.AddText(\"This is just a sample paragraph.\");\nparagraph.AddLineBreak();\nparagraph.AddText(\"The comment was added to this document.\");\nparagraph.AddComment(\"comment\", \"Jane\");"
  ],
  "params": [
    {
      "description": "The comment text.",
      "name": "sText",
      "type": "string"
    },
    {
      "description": "The author's name.",
      "name": "sAuthor",
      "optional": true,
      "type": "string"
    },
    {
      "description": "The user ID of the comment author.",
      "name": "sUserId",
      "optional": true,
      "type": "string"
    }
  ],
  "returns": {
    "description": "Returns null if the comment was not added.",
    "type": "ApiComment"
  },
  "signature": "AddComment(sText: string, sAuthor?: string, sUserId?: string): ApiComment"
}
