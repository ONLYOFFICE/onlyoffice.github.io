{
  "description": "Adds an inline container.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/document-api/ApiParagraph/Methods/AddInlineLvlSdt/",
  "examples": [
    "// How do I place a structured text field inside a paragraph in a document?\n\n// Wrap a run of text in a bounded inline control so it can be managed or restricted separately within the paragraph in a document.\n\nlet doc = Api.GetDocument();\nlet paragraph = doc.GetElement(0);\nlet inlineLvlSdt = Api.CreateInlineLvlSdt();\nlet run = Api.CreateRun();\nrun.AddText(\"This is an inline text content control.\");\ninlineLvlSdt.AddElement(run, 0);\nparagraph.AddInlineLvlSdt(inlineLvlSdt);"
  ],
  "params": [
    {
      "description": "An inline container. If undefined or null, then new class ApiInlineLvlSdt will be created and added to the paragraph.",
      "name": "oSdt",
      "type": "ApiInlineLvlSdt"
    }
  ],
  "returns": {
    "type": "ApiInlineLvlSdt"
  },
  "signature": "AddInlineLvlSdt(oSdt: ApiInlineLvlSdt): ApiInlineLvlSdt"
}
