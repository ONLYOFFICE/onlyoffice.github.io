{
  "description": "Returns a number of elements in the current paragraph.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/document-api/ApiParagraph/Methods/GetElementsCount/",
  "examples": [
    "// How do I count the elements that make up a paragraph in a document?\n\n// Track how the element count changes as new runs are added to a paragraph in a document.\n\nlet doc = Api.GetDocument();\nlet paragraph = doc.GetElement(0);\nparagraph.RemoveAllElements();\nlet run = Api.CreateRun();\nrun.AddText(\"Number of paragraph elements at this point: \");\nrun.AddTabStop();\nrun.AddText(\"\" + paragraph.GetElementsCount());\nrun.AddLineBreak();\nparagraph.AddElement(run);\nrun.AddText(\"Number of paragraph elements after we added a text run: \");\nrun.AddTabStop();\nrun.AddText(\"\" + paragraph.GetElementsCount());"
  ],
  "params": [],
  "returns": {
    "type": "number"
  },
  "signature": "GetElementsCount(): number"
}
