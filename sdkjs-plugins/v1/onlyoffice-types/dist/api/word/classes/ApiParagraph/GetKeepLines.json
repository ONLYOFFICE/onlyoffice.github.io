{
  "description": "Returns the keep lines value of the current paragraph.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/document-api/ApiParaPr/Methods/GetKeepLines/",
  "examples": [
    "// How do I check if a paragraph is set to keep all its lines on the same page in a document?\n\n// Verify the keep-lines setting before modifying pagination rules for multi-line paragraphs.\n\nlet doc = Api.GetDocument();\nlet paragraph = doc.GetElement(0);\nlet paraPr = paragraph.GetParaPr();\nparaPr.SetKeepLines(true);\nparagraph.AddText(\"This is a paragraph with all lines kept on the same page.\");\nlet keepLines = paraPr.GetKeepLines();\nlet paragraph2 = Api.CreateParagraph();\nparagraph2.AddText(\"Keep lines: \" + keepLines);\ndoc.Push(paragraph2);"
  ],
  "params": [],
  "returns": {
    "description": "Returns _true_ if all lines of the paragraph are kept on a single page,\r_false_ if they are not, or _undefined_ if the value is not set.",
    "type": "boolean | undefined"
  },
  "signature": "GetKeepLines(): boolean | undefined",
  "since": "9.4.0"
}
