{
  "description": "Selects the current paragraph.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/document-api/ApiParagraph/Methods/Select/",
  "examples": [
    "// How do I select a paragraph to apply formatting to it in a document?\n\n// Activate a paragraph selection and then italicize its text in a document.\n\nlet doc = Api.GetDocument();\nlet paragraph = doc.GetElement(0);\nparagraph.AddText(\"ONLYOFFICE Document Builder\");\nlet result = paragraph.Select();\nif (result === true) {\n\tparagraph.SetItalic(true);\n}"
  ],
  "params": [],
  "returns": {
    "type": "boolean"
  },
  "signature": "Select(): boolean"
}
