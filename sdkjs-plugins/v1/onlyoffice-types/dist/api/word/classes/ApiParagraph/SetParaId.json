{
  "description": "Specifies a unique ID for the current paragraph.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/document-api/ApiParagraph/Methods/SetParaId/",
  "examples": [
    "// How do I give a paragraph a custom ID so it can be referenced later in a document?\n\n// Tag a paragraph with a specific identifier and confirm it was applied in a document.\n\nconst doc = Api.GetDocument();\nlet paragraph = doc.GetElement(0);\nparagraph.AddText('This paragraph will have a custom ID.');\n\nconst customId = 920;\nparagraph.SetParaId(customId);\n\nparagraph = Api.CreateParagraph();\nparagraph.AddText('Custom paragraph ID was set to: ' + customId);\ndoc.Push(paragraph);\n\nconst retrievedId = paragraph.GetParaId();\nparagraph = Api.CreateParagraph();\nparagraph.AddText('Retrieved paragraph ID: ' + retrievedId);\ndoc.Push(paragraph);"
  ],
  "params": [
    {
      "description": "The numerical ID which will be specified for the current paragraph. Value MUST be greater than 0 and less than 0x80000000.",
      "name": "paraId",
      "type": "number"
    }
  ],
  "returns": {
    "type": "boolean"
  },
  "signature": "SetParaId(paraId: number): boolean",
  "since": "9.2.0"
}
