{
  "description": "Adds the new columns to the current table.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/document-api/ApiTable/Methods/AddColumns/",
  "examples": [
    "let doc = Api.GetDocument();\nlet tableStyle = doc.CreateStyle(\"CustomTableStyle\", \"table\");\ntableStyle.SetBasedOn(doc.GetStyle(\"Bordered\"));\nlet table = Api.CreateTable(3, 3);\ntable.SetWidth(\"percent\", 100);\ntable.SetStyle(tableStyle);\ndoc.Push(table);\nlet cell = table.GetCell(0, 0).GetContent().GetElement(0).AddText(\"Two new columns were added after this cell.\");\ntable.AddColumns(cell, 2, false);"
  ],
  "params": [
    {
      "description": "The cell after which the new columns will be added. If not specified, the new columns will be added at the end of the table.",
      "name": "oCell",
      "optional": true,
      "type": "ApiTableCell"
    },
    {
      "description": "Count of columns to be added.",
      "name": "nCount",
      "optional": true,
      "type": "number"
    },
    {
      "description": "Adds the new columns before (false) or after (true) the specified cell. If no cell is specified,\rthen this parameter will be ignored.",
      "name": "isBefore",
      "optional": true,
      "type": "boolean"
    }
  ],
  "returns": {
    "type": "ApiTable"
  },
  "signature": "AddColumns(oCell?: ApiTableCell, nCount?: number, isBefore?: boolean): ApiTable"
}
