{
  "description": "Adds a new row to the current table.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/document-api/ApiTable/Methods/AddRow/",
  "examples": [
    "let doc = Api.GetDocument();\nlet paragraph = doc.GetElement(0);\nparagraph.AddText(\"We create a 2x2 table and add a new row, so that it becomes 2x3:\");\nlet tableStyle = doc.CreateStyle(\"CustomTableStyle\", \"table\");\ntableStyle.SetBasedOn(doc.GetStyle(\"Bordered\"));\nlet table = Api.CreateTable(2, 2);\ntable.SetWidth(\"percent\", 100);\ntable.SetStyle(tableStyle);\ntable.AddRow(table.GetRow(1).GetCell(0), true);\ndoc.Push(table);"
  ],
  "params": [
    {
      "description": "The cell after which a new row will be added. If not specified, a new row will\rbe added at the end of the table.",
      "name": "oCell",
      "optional": true,
      "type": "ApiTableCell"
    },
    {
      "description": "Adds a new row before (false) or after (true) the specified cell. If no cell is specified, then\rthis parameter will be ignored.",
      "name": "isBefore",
      "optional": true,
      "type": "boolean"
    }
  ],
  "returns": {
    "type": "ApiTableRow"
  },
  "signature": "AddRow(oCell?: ApiTableCell, isBefore?: boolean): ApiTableRow"
}
