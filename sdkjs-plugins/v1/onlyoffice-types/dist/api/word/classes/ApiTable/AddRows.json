{
  "description": "Adds the new rows to the current table.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/document-api/ApiTable/Methods/AddRows/",
  "examples": [
    "// How do I add several rows to a table in a document?\n\n// Grow a table by placing a batch of new rows after a selected cell in a document.\n\nlet doc = Api.GetDocument();\nlet tableStyle = doc.CreateStyle(\"CustomTableStyle\", \"table\");\ntableStyle.SetBasedOn(doc.GetStyle(\"Bordered\"));\nlet table = Api.CreateTable(3, 3);\ntable.SetWidth(\"percent\", 100);\ntable.SetStyle(tableStyle);\ndoc.Push(table);\nlet cell = table.GetCell(0, 0).GetContent().GetElement(0).AddText(\"Two new rows were added after this cell.\");\ntable.AddRows(cell, 2, false);"
  ],
  "params": [
    {
      "description": "The cell after which the new rows will be added. If not specified, the new rows will\rbe added at the end of the table.",
      "name": "oCell",
      "optional": true,
      "type": "ApiTableCell"
    },
    {
      "description": "Count of rows to be added.",
      "name": "nCount",
      "optional": true,
      "type": "number"
    },
    {
      "description": "Adds the new rows before (false) or after (true) the specified cell. If no cell is specified, then\rthis parameter will be ignored.",
      "name": "isBefore",
      "optional": true,
      "type": "boolean"
    }
  ],
  "returns": {
    "type": "ApiTable"
  },
  "signature": "AddRows(oCell?: ApiTableCell, nCount?: number, isBefore?: boolean): ApiTable"
}
