{
  "description": "Returns a cell by its position.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/document-api/ApiTable/Methods/GetCell/",
  "examples": [
    "let doc = Api.GetDocument();\nlet tableStyle = doc.CreateStyle(\"CustomTableStyle\", \"table\");\ntableStyle.SetBasedOn(doc.GetStyle(\"Bordered\"));\nlet table = Api.CreateTable(3, 3);\ntable.SetWidth(\"percent\", 100);\ntable.SetStyle(tableStyle);\ndoc.Push(table);\nlet cell = table.GetCell(0, 0);\ncell.GetContent().GetElement(0).AddText(\"Cell #1\");"
  ],
  "params": [
    {
      "description": "The row index in the current table.",
      "name": "rowIndex",
      "type": "number"
    },
    {
      "description": "The cell index in the specified row.",
      "name": "cellIndex",
      "type": "number"
    }
  ],
  "returns": {
    "type": "ApiTableCell"
  },
  "signature": "GetCell(rowIndex: number, cellIndex: number): ApiTableCell"
}
