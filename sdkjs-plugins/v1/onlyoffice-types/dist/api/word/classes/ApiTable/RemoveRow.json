{
  "description": "Removes a table row with a specified cell.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/document-api/ApiTable/Methods/RemoveRow/",
  "examples": [
    "let doc = Api.GetDocument();\nlet paragraph = doc.GetElement(0);\nparagraph.AddText(\"We create a 3x3 table and remove one row (the second one), so that it becomes 3x2:\");\nlet tableStyle = doc.CreateStyle(\"CustomTableStyle\", \"table\");\ntableStyle.SetBasedOn(doc.GetStyle(\"Bordered\"));\nlet table = Api.CreateTable(3, 3);\ntable.SetWidth(\"percent\", 100);\ntable.SetStyle(tableStyle);\nlet cell = table.GetRow(1).GetCell(0);\ntable.RemoveRow(cell);\ndoc.Push(table);"
  ],
  "params": [
    {
      "description": "The cell which is placed in the row that will be removed.",
      "name": "oCell",
      "type": "ApiTableCell"
    }
  ],
  "returns": {
    "description": "result of deletion",
    "type": "boolean"
  },
  "signature": "RemoveRow(oCell: ApiTableCell): boolean"
}
