{
  "description": "Sets the table paddings.\nIf table is inline, then only left padding is applied.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/document-api/ApiTable/Methods/SetPaddings/",
  "examples": [
    "let doc = Api.GetDocument();\nlet tableStyle = doc.CreateStyle(\"CustomTableStyle\", \"table\");\ntableStyle.SetBasedOn(doc.GetStyle(\"Bordered\"));\nlet table = Api.CreateTable(3, 3);\ntable.SetStyle(tableStyle);\ndoc.Push(table);\ntable.GetCell(0, 0).GetContent().GetElement(0).AddText(\"This is just a sample text.\");\ntable.SetPaddings(10, 10, 5, 5);"
  ],
  "params": [
    {
      "description": "Left padding.",
      "name": "nLeft",
      "type": "number"
    },
    {
      "description": "Top padding.",
      "name": "nTop",
      "type": "number"
    },
    {
      "description": "Right padding.",
      "name": "nRight",
      "type": "number"
    },
    {
      "description": "Bottom padding.",
      "name": "nBottom",
      "type": "number"
    }
  ],
  "returns": {
    "description": "returns true.",
    "type": "boolean"
  },
  "signature": "SetPaddings(nLeft: number, nTop: number, nRight: number, nBottom: number): boolean"
}
