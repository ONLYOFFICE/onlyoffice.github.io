{
  "description": "Specifies the shading which is applied to the extents of the current table.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/document-api/ApiTablePr/Methods/SetShd/",
  "examples": [
    "// How do I apply a background shade to a whole table in a document?\n\n// Give a table a colored backdrop to highlight it visually in a document.\n\nlet doc = Api.GetDocument();\nlet paragraph = doc.GetElement(0);\nparagraph.AddText(\"We added an orange shading to the table:\");\nlet tableStyle = doc.CreateStyle(\"CustomTableStyle\", \"table\");\ntableStyle.SetBasedOn(doc.GetStyle(\"Bordered\"));\nlet tablePr = tableStyle.GetTablePr();\nlet table = Api.CreateTable(3, 3);\ntablePr.SetShd(\"clear\", Api.HexColor('#FF6F3D'));\ntable.SetTableLook(true, true, true, true, false, false);\ntable.SetStyle(tableStyle);\ndoc.Push(table);"
  ],
  "params": [
    {
      "description": "The shading type applied to the extents of the current table.",
      "name": "sType",
      "type": "ShdType"
    },
    {
      "description": "Red color component value.",
      "name": "r",
      "type": "number"
    },
    {
      "description": "Green color component value.",
      "name": "g",
      "type": "number"
    },
    {
      "description": "Blue color component value.",
      "name": "b",
      "type": "number"
    },
    {
      "description": "The true value disables the SetShd method use.",
      "name": "isAuto",
      "optional": true,
      "type": "boolean"
    }
  ],
  "returns": {
    "type": "boolean"
  },
  "signature": "SetShd(sType: ShdType, r: number, g: number, b: number, isAuto?: boolean): boolean"
}
