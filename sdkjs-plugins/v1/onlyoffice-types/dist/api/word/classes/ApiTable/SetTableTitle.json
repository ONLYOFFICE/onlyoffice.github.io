{
  "description": "Sets the table title (caption).",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/document-api/ApiTablePr/Methods/SetTableTitle/",
  "examples": [
    "// How do I give a table a named title so it can be identified in a document?\n\n// Label a table with a title string to make it easier to reference in a document.\n\nlet doc = Api.GetDocument();\nlet tableStyle = doc.CreateStyle(\"CustomTableStyle\", \"table\");\ntableStyle.SetBasedOn(doc.GetStyle(\"Bordered\"));\nlet tablePr = tableStyle.GetTablePr();\nlet table = Api.CreateTable(3, 3);\ntable.SetWidth(\"percent\", 100);\ntablePr.SetTableTitle(\"Table 1\");\ntable.SetStyle(tableStyle);\nlet paragraph = doc.GetElement(0);\nparagraph.AddText(\"Table title: \" + tablePr.GetTableTitle());\ndoc.Push(table);"
  ],
  "params": [
    {
      "description": "The table title to be set.",
      "name": "sTitle",
      "type": "string"
    }
  ],
  "returns": {
    "type": "boolean"
  },
  "signature": "SetTableTitle(sTitle: string): boolean"
}
