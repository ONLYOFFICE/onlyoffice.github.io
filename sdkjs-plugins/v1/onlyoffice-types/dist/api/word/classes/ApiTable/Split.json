{
  "description": "Splits the cell into a given number of rows and columns.",
  "docsUrl": "https://api.onlyoffice.com/docs/office-api/usage-api/document-api/ApiTable/Methods/Split/",
  "examples": [
    "// How do I break a single cell into multiple rows and columns in a document?\n\n// Subdivide a cell to create a nested grid structure inside a table in a document.\n\nlet doc = Api.GetDocument();\nlet tableStyle = doc.CreateStyle(\"CustomTableStyle\", \"table\");\ntableStyle.SetBasedOn(doc.GetStyle(\"Bordered\"));\nlet table = Api.CreateTable(3, 3);\ntable.SetWidth(\"percent\", 100);\ntable.SetStyle(tableStyle);\ndoc.Push(table);\nlet cell = table.GetCell(0, 0);\ntable.Split(cell, 2, 2);"
  ],
  "params": [
    {
      "description": "The cell which will be split.",
      "name": "oCell",
      "optional": true,
      "type": "ApiTableCell"
    },
    {
      "description": "Count of rows into which the cell will be split.",
      "name": "nRow",
      "optional": true,
      "type": "number"
    },
    {
      "description": "Count of columns into which the cell will be split.",
      "name": "nCol",
      "optional": true,
      "type": "number"
    }
  ],
  "returns": {
    "description": "returns null if can't split.",
    "type": "ApiTable | null"
  },
  "signature": "Split(oCell?: ApiTableCell, nRow?: number, nCol?: number): ApiTable | null"
}
