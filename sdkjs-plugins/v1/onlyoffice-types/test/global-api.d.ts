// The documentation smoke tests cover executeMethod call shapes across editors in one TypeScript program.
// Editor-specific callCommand types are tested through the /word, /cell, /slide, and /pdf entry points.
declare var Api: any;
